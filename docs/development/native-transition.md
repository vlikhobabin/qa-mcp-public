# Return to product delivery using native OpenSpec

The operator's 2026-09-09 direction is to adopt the ordinary ChangeRail scheme
being developed in `meta-mcp` and return to qa-mcp product corrections. This
supersedes the old continuation recommendation to make the whole CHRL-FIX queue
the default next work. The subsequent explicit operator instruction authorizes finalization, commit and push of all current repository changes.

## Ordinary route

One explicitly native card owns one stock OpenSpec change. Prepare artifacts,
run `native-accept --dry-run`, admit the plan, then deliver from a clean primary
main with satisfied dependencies. The runner owns group sessions, QA evidence,
semantic sync, independent review, exact archive, final verification and publish.
Numerical limits remain informational under QA's existing operator policy.

The meta-mcp source is a working snapshot, not an assumed released dependency.
`.changerail/native-source.json` records its HEAD and the consulted file hashes.
The local QA adapter preserves evidence roles, the non-live coverage floor,
native runtime authorization and the existing publisher. Meta-mcp's product
checks and historical exception engines are not imported.

## Concrete next product card

`openspec/board/1.backlog/oss-fix-02-reject-unproven-project-attach.md` now links
`oss-fix-02-native-bound-attach-containment`, with proposal, delta spec, design
and two ordered task groups. Its C1-C3 map refusal ordering, trusted provenance
review and compatibility proof. This is preparation only: tasks remain unchecked
and the card remains backlog. The source card at Git commit e7f7d4f retains the
old board-only plan; its containment scope has not expanded.

Other unaccepted product drafts are converted when selected. Do not bulk-add a
lifecycle marker to frozen or accepted historical cards. The superseded FIX-04
and FIX-09 aggregates remain non-executable history.

## One-off reconciliation — 2026-09-09

The operator explicitly authorized finalization and publication of all current
repository changes. The publication candidate includes FIX-01's amended product
code/tests/specs, all previously accumulated process changes, the native
adaptation and FIX-02 planning. Ignored runtime evidence, credentials, installed
dependencies and local captures remain outside Git.

FIX-01 closes at `openspec/board/4.done/oss-fix-01-freeze-physical-target-configuration.md`
through this one-off reconciliation. Its stopped pilot and original receipts
remain unchanged; no old GO is promoted and no runner context is manufactured.
The four frozen historical board records remain byte-identical. The general
CHRL-FIX-02 finalizer remains disabled and is not a product prerequisite.

Fresh independent review covers the combined candidate against `e7f7d4f`, with
repairs for verbose final condition proof, archived recovery initialization,
Scenario ownership in the template and exact synthetic scanner allowances.
Checks and all attempts are retained under
`.runtime/changerail/publication-20260909/`. Publication requires a fresh GO for
the final candidate and a matching staged tree. The containing commit is the
source identity; retained local evidence is supplementary context.

After this commit is published and main is clean, the next product step is the
prepared native FIX-02 plan. Its dependency now points to FIX-01's done path.
Admission and delivery still use the ordinary native commands and their own
scope/authority. Further product drafts convert when selected; Linux/Windows
FIX-11/12 and the release train keep their separate gates.

## Evidence limits

Tool tests establish deterministic behavior with model calls substituted at the
session boundary. The real local OpenSpec/Git integration checks establish plan
admission, immutable history, semantic-sync binding and exact archive behavior.
The one-off publication review and product coverage are separate checks retained
with this reconciliation. No completed real model native delivery, Windows-native
behavior or live TestClient qualification is claimed.

# I15 Session-1 Admission And S4 Isolation Decision

## Decision

I15 is complete as a bounded `NOT-VERIFIABLE` evidence decision. The one
authorized original-route canary produced no typed Session-1 receipt, so the
candidate gate remained closed and S4 invocation count is exactly `0`. No
host, identity, session, platform, target, fixture, candidate or argv was
substituted, and I13 remains active, unarchived and uncertified.

## Exact Source And Preflight

All five product/test blobs exactly match published I11 and have zero path diff
from `964e29f`. The retained candidate hash matches the published lineage.
Exact host/principal/controller session, interactive Session 1 presence,
platform, target and initial configuration hashes passed. Exact task, stage,
process and 1C counts were zero; the initial last-access hash and typed
job/desktop/transport owner zero-state were retained. Because the canary failed,
the conditional runtime fixture and S4 argv gates were never reached or claimed.

## Admission And Isolation

Exactly one canary task was created and started through the original interactive
route. Its bounded controller returned no receipt, retained unchanged unrelated
task counts, unregistered its exact task and reported candidate invocation count
`0`. Per the fail-closed gate, no candidate or fixture was staged and no S4
candidate, I13 row or certification matrix was invoked.

## Topology Attribution

The protected task count remained unchanged across the canary controller. After
cleanup, the exact published-I14 normalization returned count `291` and the
historical before hash, not the transient historical after hash. The Task
Scheduler operational log exists but is disabled, and no pre-start identity-set
hash was retained by the canary controller. Therefore the started-route evidence
cannot distinguish harness/start behavior from external drift. Topology remains
`NOT-VERIFIABLE`; no cause or ownership violation is inferred.

## Cleanup And Privacy

The controller had already removed its exact task, so both explicit task/process
cleanup passes removed nothing and found zero residue. Exact stage cleanup
removed one stage on its first pass and nothing on its second. Every initial
configuration content, ACL, creation, write, access and attributes hash matches
after cleanup; exact task/stage/process/job/desktop/transport and 1C counts are
zero. No foreign target was selected.

No raw UI, candidate output, screenshot, credential, connection string, broad
dump or task name is retained. Ephemeral scripts were removed locally and the
remote exact stage is absent. The tracked payload changes only bounded evidence,
OpenSpec and board/roadmap summaries; product/test and license paths are
unchanged.

## Outcome

I15 does not authorize another attempt. A separately authorized successor may
proceed only after exact original-route typed Session-1 admission is independently
available. I13, the S4-R1 parent and S7 remain blocked.

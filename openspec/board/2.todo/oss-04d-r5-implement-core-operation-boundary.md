# Implement Core Operation Boundary

## Status
2.todo

## Owner
qa-mcp

## Series
oss-04d-r5

## Order Index
4034.86

## OpenSpec Stage
blocked / exhausted review

## Parent Epic
- `openspec/board/4.done/oss-04-bind-testclient-to-declared-project-runtime-target.md`

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Credential or mutation authority: `no`
- Repeated defect class: `yes`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `{"authorization_card":"openspec/board/4.done/oss-04d-a2-authorize-core-operation-boundary-replacement.md","authorization_id":"oss-04d-a2-authorize-core-operation-boundary-replacement"}`

## Goal
Implement the R4-R1 context-local evidence ledger and total bounded public
normalizer as one core-only payload.

## Acceptance
- Application-owned per-operation ledger scopes isolate concurrent contexts;
  executor data never grants artifact path authority.
- Path candidates separately fail across operation, target, session, binding,
  fingerprint and generation while paired exact controls preserve.
- Depth/item/node/string and complete canonical UTF-8 JSON limits are exact;
  overflow uses one fixed bounded typed fallback.
- Core URL tests cover credential path, `user@` and `user:pass@` authority
  hostiles at exact depths 1–5. Paired benign controls preserve canonically at
  depths 1–4 and reject only as non-fixed at depth 5.
- Artifact/value/error reconstruction is total and secret-safe.
- Added production lines are at most 300; no routing/lifecycle/new authority or
  wire protocol is added.

## Scope
- Core ledger, normalizer, provenance/URL admission and direct RED matrix.
- No MCP/ScenarioRunner integration, lifecycle or live runtime.

## Change Set
none; fast-forward required.

## Depends On
- `oss-04d-r4-r1-replace-exhausted-boundary-investigation`
- `oss-04d-a2-authorize-core-operation-boundary-replacement`

## Blocks
- none; R5 cannot publish and is superseded by R5-R2 → A4 → R7.

## Verify
- Test-first isolation/provenance/structure/URL/artifact RED and green matrix.
- Focused/full non-live, coverage, compile, strict OpenSpec, LOC, scope/diff.
- Exact-source Windows offline path/serialization proof with owned cleanup.
- Fresh independent review.

## Result
Unpublished runtime payload exhausted independent review cycle 3 at rescue
budget `2/2`. Exact payload is retained in the named evidence-only stash
`oss04d-r5-exhausted-review-payload-20260826` and MUST NOT be restored or
published wholesale.

## Next
- Do not deliver R5. Follow R5-R2 → A4 → R7 → A5 → R8.

## Log
- 2026-08-25 created by R4-R1 as the bounded core successor.
- 2026-08-26 blocked after exhausted review; superseded by the positive-schema
  replacement chain.

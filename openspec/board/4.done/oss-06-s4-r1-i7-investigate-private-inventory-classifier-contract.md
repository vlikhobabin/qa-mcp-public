# Investigate Private Inventory Classifier Authorization Boundary

## Status
4.done

## Owner
unassigned

## Series
oss-06-s4-r1-i7

## Order Index
405.1019

## OpenSpec Stage
archived

## Parent Card
- `openspec/board/2.todo/oss-06-s4-r1-stabilize-hidden-direct-execute-observation-lifecycle.md`

## Source
- Operator handoff identifies exact I4 as the successor that needs the
  repeated-defect graph; its local implementation/preflight claims are not
  treated as publishable I7 evidence.
- Published I3 inventory-boundary decision and I6 private fenced seam at clean
  baseline `150e095547ed835a821993218f8c21155bd5ae39`.
- Current ChangeRail authorization parser and published investigation examples.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Repeated defect class: `no`
- Credential or mutation authority: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`

## Summary
Publish the tracked design decision required by ChangeRail before the exact I4
repeated-defect payload may receive a separate bounded authorization. Decide
whether the published I3/I6 private classifier shape is a sufficiently
simplified successor boundary, bind exact I4's path and required future
verification floor, and grant no product, runtime, live, review or publication
authority in this card.

## Acceptance
- Reproduce the typed blocker from the current ChangeRail contract: prose
  lineage cannot clear `Repeated defect class: yes`; the exact successor needs
  one unchanged tracked `4.done` investigation with reciprocal card links and
  a separate unchanged tracked `4.done` authorization source.
- Bind only successor id
  `oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause` at
  authorization-time path
  `openspec/board/3.inprogress/oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause.md`.
- Decide whether the I4 boundary is sufficiently simplified: one private,
  behavior-neutral closed-vocabulary projection at the two existing I6 fenced
  boundaries, with no additional Windows call, retry, fallback, action, public
  route, marker derivation, authority or wire field.
- Record the exact future authorization source object with the minimum valid
  machine ceiling `301` and
  `allow_new_authority_or_wire_protocol: false`; do not create or publish that
  authorization card in this change.
- Preserve I4's five-path ceiling and hostile offline verification floor,
  including 15 terminal rows, privacy, call/fence counts, zero action, focused
  and full Go tests/vet, Linux repository tests, deterministic non-executed
  Windows cross-builds, strict OpenSpec, scope and whitespace checks.
- Change only this card's OpenSpec decision artifacts, synced capability and
  board metadata. Add no product/test code and import no bytes from the
  retained unpublished I4 workspace.
- Remain offline: no Windows or 1C execution, live/endpoint/target access, SSH,
  or use of the retained evidence-only historical-user identity.
- Preserve Apache-2.0 licensing and stop if the decision would authorize raw
  error parsing, another Windows operation, changed runtime behavior, a public
  surface, a live retry or any successor other than exact I4.

## Non-Goals
- Implementing, copying, reviewing or publishing the retained I4 payload.
- Creating the separate authorization card or updating I4's authorization
  reference.
- Raising ChangeRail's global production-LOC ceiling or granting a reusable
  repeated-defect waiver.
- Any Windows, 1C, live, endpoint, target, SSH or historical-user operation.

## Investigation Decision
Exact I4 is the only eligible bounded successor for this repeated defect class,
conditional on its later authorization/composition session proving the
requirements below. The selected shape is one behavior-neutral
closed-vocabulary cause projection at the two published I6 private fenced
boundaries. It must preserve the existing two inventory calls, pre/post fence
counts, polling, fail-closed outcomes, passive-UIA admission and zero action,
and must add no Windows operation, retry, fallback, wait, process/job/desktop
mutation, marker derivation, public route, authority or wire field.

The exact successor is
`oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause` at
authorization-time path
`openspec/board/3.inprogress/oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause.md`.
The decision does not relabel its true `Repeated defect class: yes` lineage.
Instead it authorizes a later separate metadata-only source to satisfy the
machine graph for this one payload.

The minimum valid machine ceiling is `301`. Before I8 is published or I4 enters
review, the responsible fresh sessions must independently establish that the
exact successor owns at most five private Go/test paths and no more than 301
production-counted lines, then bind and preserve those blob identities while
composing metadata. I7 does not certify the operator-retained local candidate
or any prior test/preflight outcome. New authority or wire protocol remains
forbidden. If exact I4 changes runtime behavior, adds a product/test path or
operation, exceeds `301`, changes authority/wire surface, weakens its hostile
verification floor or requires live evidence, this decision does not apply and
a new split/investigation is required.

The preserved verification floor is the identical 15-row hostile RED/GREEN
matrix; closed cause, malformed/conflicting-state, hidden/operator/isolation,
privacy and connected-projection checks; exact inventory/fence/admission/action
counts; focused/full Go tests and vet; Linux repository tests; deterministic
amd64/386 Windows cross-builds without execution; strict OpenSpec; explicit
at-most-five-path scope; privacy and untracked-aware whitespace checks. These
are mandatory future admission conditions, not claims that a retained local
candidate already passed them. The later authorization/composition sessions
must retain public-safe evidence for the exact payload and rerun deterministic
preflight plus fresh independent review.

## Prepared Authorization Source Data
The next and only eligible dependency is the separate card
`oss-06-s4-r1-i8-authorize-private-inventory-classifier-payload`. After this
investigation is published unchanged in `4.done`, that card must have exact
`Depends On` relation
`oss-06-s4-r1-i7-investigate-private-inventory-classifier-contract` and exact
`Investigation authorization` JSON:

```json
{"investigation_card":"openspec/board/4.done/oss-06-s4-r1-i7-investigate-private-inventory-classifier-contract.md","investigation_id":"oss-06-s4-r1-i7-investigate-private-inventory-classifier-contract","successor_card":"openspec/board/3.inprogress/oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause.md","successor_id":"oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause","production_loc_ceiling":301,"allow_new_authority_or_wire_protocol":false}
```

This investigation does not create I8. Only after I8 is separately reviewed
and published unchanged in `4.done` may I4 add exact `Depends On` relation to
this investigation and set its review field to:

```json
{"authorization_card":"openspec/board/4.done/oss-06-s4-r1-i8-authorize-private-inventory-classifier-payload.md","authorization_id":"oss-06-s4-r1-i8-authorize-private-inventory-classifier-payload"}
```

Any additional/missing field, different path/id, stale/untracked dependency or
absent reciprocal relation remains fail-closed and cannot authorize I4.

## Blocks
- `oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause`

## Change Set
1. `investigate-private-inventory-classifier-authorization-boundary` -
   `openspec/changes/archive/2026-09-01-investigate-private-inventory-classifier-authorization-boundary/`

## Verify
- `bin/openspec validate
  investigate-private-inventory-classifier-authorization-boundary --strict`
  and `bin/openspec validate
  qa-mcp-private-inventory-classifier-authorization-decision --strict` passed.
- `bin/openspec validate --all --strict` passed `58/58` before archive.
- Focused literal audit found the exact I4 id/path, investigation path/id,
  future I8 path/id, ceiling `301` and authority/wire flag `false`; the card's
  `Blocks` relation names exact I4.
- `git status --porcelain` prefix audit passed with only three docs/OpenSpec
  roots and HEAD remained clean published baseline `150e095`; no product,
  test, fixture or runtime-evidence path was imported from retained I4.
- Focused privacy/forbidden-identity scan, `git diff --check` and explicit
  untracked-file trailing-whitespace scan passed with zero findings.
- Windows-native/test-first/live verification is not applicable because this
  payload changes no runtime/test surface; Windows, 1C, live, endpoint, target,
  SSH and historical-user operations were prohibited and did not run.

## Archive
- `openspec/changes/archive/2026-09-01-investigate-private-inventory-classifier-authorization-boundary/`

## Related
- `openspec/changes/archive/2026-09-01-investigate-private-inventory-classifier-authorization-boundary/`
- `openspec/board/4.done/oss-06-s4-r1-i3-decide-hidden-desktop-inventory-observation-boundary.md`
- `openspec/board/4.done/oss-06-s4-r1-i6-publish-clean-head-private-fenced-seam.md`

## Result
Decision-only investigation completed, synced and archived. Exact I4 is the
only conditionally eligible bounded repeated-defect successor; one separate I8
authorization source is prepared with ceiling `301` and authority/wire flag
`false`. I7 makes no certification claim about an operator-retained I4 payload
or prior I4 test/preflight outcome. No product, test, runtime evidence or
external action entered the payload. The card remains in `3.inprogress` for its
required independent review and publication.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `investigate-private-inventory-classifier-authorization-boundary`

### Why
Exact I4 is the designated successor for the published I3/I6 private classifier
shape, but a declared repeated defect class requires the exact machine-readable
published investigation/authorization graph before its own payload may enter
review. I7 does not certify any local I4 implementation or prior verification.

### Goal
Publish the narrow design decision and exact authorization handoff without
changing product code or authorizing I4 directly.

### Scope
- Current ChangeRail machine contract and clean published I3/I6 lineage.
- Exact I4 successor identity, private classifier boundary and verification
  floor.
- One prepared future authorization object using ceiling `301` and protocol
  allowance `false`.
- Documentation/OpenSpec/board metadata only.

### Acceptance
- The decision concludes whether exact I4 is the bounded simplification.
- The future authorization handoff is exact, non-reusable and independently
  checkable.
- This payload contains no implementation or external action.

### Depends On
- `oss-06-s4-r1-i3-decide-hidden-desktop-inventory-observation-boundary`
- `oss-06-s4-r1-i6-publish-clean-head-private-fenced-seam`

### Related
- `openspec/changes/archive/2026-09-01-investigate-private-inventory-classifier-authorization-boundary/`

## Log
- 2026-09-02 created on clean published baseline `150e095` for exact I4's
  repeated-defect machine-contract dependency; no local I4 implementation or
  preflight outcome is treated as I7 acceptance evidence.
- 2026-09-02 fast-forwarded one decision-only change with exact I4 identity,
  minimum machine ceiling `301`, closed authority/wire flag and a separate I8
  authorization handoff; no implementation or external action was planned.
- 2026-09-02 recorded the bounded I4 decision and exact future six-field/two-
  field machine data, synced the decision capability and passed strict
  OpenSpec `58/58`, exact relation, docs-only scope, privacy and whitespace
  verification without product changes or external operations.
- 2026-09-02 archived the fully completed change after the already-synced spec
  duplicate was correctly handled with `--skip-specs`; post-archive strict
  OpenSpec passed `57/57`. Card remains in `3.inprogress` for independent
  review.
- 2026-09-02 review cycle 1 returned `NO-GO` because I7 stated local I4
  five-path/LOC/test facts without immutable card-owned evidence. Rescue 1
  removed those certification claims and made payload size/blob identity plus
  the complete hostile/offline floor mandatory future I8/I4 admission
  conditions; no I4 bytes or new evidence were imported.
- 2026-09-01T22:18:56Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.

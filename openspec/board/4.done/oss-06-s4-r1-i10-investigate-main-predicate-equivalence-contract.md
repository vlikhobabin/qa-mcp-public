# Investigate Main-Predicate Equivalence Contract

## Status
4.done

## Owner
unassigned

## Series
oss-06-s4-r1-i10

## Order Index
405.10193

## OpenSpec Stage
archived

## Parent Card
- `openspec/board/2.todo/oss-06-s4-r1-stabilize-hidden-direct-execute-observation-lifecycle.md`

## Source
- Safe published baseline and `origin/main`:
  `2517ed6b55a6efed8f2cd84b46f8dea18090fd36`.
- Rejected/unpublished I4
  `oss-06-s4-r1-i4-classify-hidden-desktop-inventory-refusal-cause`.
- Published I7 investigation and published I8 exact authorization.
- Rejected/unpublished I9
  `oss-06-s4-r1-i9-refuse-malformed-main-inventory-classification`.
- Canonical I9 review-cycle-3 verdict at reviewed tree
  `cda26645dc8f0e3cc1bab8761125b0de1af0fb9c`; the later contaminated I9
  working tree is not evidence for the reviewed payload.

## Review
- Risk tier: `ordinary`
- Milestone audit: `no`
- New authority or wire protocol: `no`
- Repeated defect class: `no`
- Credential or mutation authority: `no`
- Live admission: `no`
- Final certification: `no`
- Published investigation authorization: `none`

## Summary
Publish the investigation/design boundary required after rejected I4 and its
linked I9 replacement repeated the same predicate-equivalence invariant class.
Enumerate the complete exact-main predicate and connected-boundary order,
define a call-neutral admission-result/pure-replay contract, close the outcome
and hostile-oracle matrices, and decide whether a separately authorized I11
implementation attempt fits the inherited five-path/301-production-LOC
envelope. This card changes documentation and OpenSpec only.

## Acceptance
- Record I4 and I9 as rejected and unpublished evidence, never as completed or
  published cards; bind I9 claims only to its canonical cycle-1 verdict,
  cycle history, evidence index and cycle-3 verdict/tree.
- Enumerate every expected-predicate and inventory-row validity check and its
  evaluation order for unchanged `admitHiddenDirectMainWindow`, first-boundary
  `admitExactHiddenWindow`, and second-boundary readmission, including the
  `len(ClassName) <= 256` rule and empty inventory.
- Define a closed equivalence contract that reuses an admission result or pure
  membership replay without any extra membership or Windows call and that
  distinguishes exact, true absence and invalid/ambiguous state without a
  synthesized success cause.
- Define predecessor invariants for external calls, polling, sleeps, fences,
  UIA ordering, terminal outcomes, privacy and zero action at both connected
  boundaries.
- Define connected RED/GREEN matrices whose identical rows fail when either
  projection, predicate rule, call-count oracle or first-decision binding is
  disconnected and pass only for the closed contract.
- Decide whether the published I8 five-product/test-path and
  301-production-LOC envelope remains sufficient, distinguishing quantitative
  sufficiency from I8's exact non-reusable I4 authorization identity.
- If supported, publish one machine-readable bounded decision, prepare a
  separate exact authorization-source card, and prepare an apply-ready I11
  successor with exact paths, LOC ceiling, lineage, acceptance, proof floor
  and fresh-review requirement; otherwise record the precise scope or
  authority gap.
- Update the S4-R1 parent roadmap with I4/I9 rejected-unpublished lineage and
  the next step while keeping the parent blocked and unimplemented.
- Change no file under `host-agent/` product or test surfaces and add no public
  API, route, wire field, runtime authority or classifier product code.
- Preserve Apache-2.0 and remain offline Linux source/evidence-only: no
  Windows, PowerShell, 1C, endpoint, lab/live/action, S5, S7 or SSH operation.

## Non-Goals
- Continuing, rescuing, completing or publishing I4 or I9.
- Implementing the classifier or changing any host-agent product/test file.
- Reusing I8 as machine authorization for a successor other than exact I4.
- Windows/PowerShell/1C execution, endpoint or target access, lab/live/action
  work, S5/S7 work, SSH or external side effects.

## Depends On
- `oss-06-s4-r1-i7-investigate-private-inventory-classifier-contract`
- `oss-06-s4-r1-i8-authorize-private-inventory-classifier-payload`

## Blocks
- `oss-06-s4-r1-i11-enforce-main-predicate-equivalence-contract`

## Change Set
1. `investigate-main-predicate-equivalence-contract`

## Verify
- `bin/openspec validate investigate-main-predicate-equivalence-contract
  --strict`, capability strict validation and `bin/openspec validate --all
  --strict` passed pre-archive (`60/60`).
- `jq -e` decision/graph assertions passed for schema, safe baseline,
  rejected-unpublished I4/I9, reviewed I9 tree, five paths, `247 < 301`, exact
  I10/I10a/I11 graph and closed authority/wire flag.
- Combined `git diff --name-only HEAD` plus untracked inventory proved every
  path under the exact decision docs or `openspec/`; `host-agent/` diff is
  empty and `git diff --quiet HEAD -- LICENSE` passed.
- Focused public-surface scan of every changed/untracked path found no local
  suite path, private endpoint/host identity, private-key marker or token-like
  GitHub prefix; parent status remains `2.todo` with explicit `Blocked` result.
- `git diff --check` and explicit untracked trailing-whitespace scan passed.
- Windows-native/test-first/live verification is prohibited and not applicable
  because the payload changes no product/test/runtime behavior.

## Archive
- `openspec/changes/archive/2026-09-02-investigate-main-predicate-equivalence-contract/`

## Related
- `openspec/board/4.done/oss-06-s4-r1-i7-investigate-private-inventory-classifier-contract.md`
- `openspec/board/4.done/oss-06-s4-r1-i8-authorize-private-inventory-classifier-payload.md`
- `openspec/board/2.todo/oss-06-s4-r1-stabilize-hidden-direct-execute-observation-lifecycle.md`

## Result
The investigation supports one further implementation attempt only inside the
same five product/test paths and a complete 301-added-production-LOC ceiling.
Published I8 is quantitatively informative but not reusable authority because
it binds exact I4. I10 therefore prepares a separate exact I10a authorization
source and an I11 card; I11 remains blocked until I10a is independently
published. No classifier, product/test change or external operation is
included.

Reviewed payload finalized through ChangeRail scoped publish; exact payload and published commit ledger is retained in the ignored delivery manifest.

## Next
- done

## Change 1: `investigate-main-predicate-equivalence-contract`

### Why
Rejected I4 collapsed broad admission errors into successful absence. Rejected
I9 then changed the predecessor's first-boundary behavior, re-traversed the
external membership predicate, and omitted the unchanged class-length
predicate from its pure taxonomy replay. A complete design contract is
required before any further implementation attempt.

### Goal
Publish a closed, independently auditable predicate-equivalence decision and
prepare, only when supported by the findings, a separately authorized bounded
I11 handoff.

### Scope
- Published source plus immutable read-only I4/I9 review evidence.
- Exact source predicates, connected ordering and call/control invariants.
- Closed outcome and connected RED/GREEN matrices.
- Machine-readable decision, successor/authorization planning and parent
  roadmap metadata.
- Documentation/OpenSpec only.

### Acceptance
- Every card-level acceptance criterion is represented by a normative
  requirement or task and verified by a named offline command/outcome.
- The decision distinguishes quantitative scope sufficiency from exact
  machine authorization and fails closed on either mismatch.
- No implementation or external operation enters the payload.

### Depends On
- none

### Related
- `openspec/changes/investigate-main-predicate-equivalence-contract/`

## Log
- 2026-09-02 created on safe published baseline `2517ed6` after two linked
  rejected/unpublished implementation cards repeated the predicate-equivalence
  invariant; no third same-card rescue or classifier implementation is
  authorized.
- 2026-09-02 fast-forwarded one docs/OpenSpec-only investigation change with
  complete predicate, pure-replay, predecessor-invariant and connected-matrix
  requirements; no product/test or external operation was planned.
- 2026-09-02 recorded the closed first/second predicate and provenance order,
  exact/absent/invalid replay contract, predecessor call/control invariants,
  connected RED/GREEN matrices, quantitative five-path/301-LOC decision and
  new exact I10a/I11 handoff. Pre-archive strict OpenSpec `60/60`, JSON graph,
  docs-only scope, parent-blocked, public-surface, license and whitespace gates
  passed; Windows/live verification remained prohibited and not applicable.
- 2026-09-02 archived the completed decision change with already-synced specs
  using `--skip-specs`; post-archive strict OpenSpec passed `59/59`. I10 remains
  in `3.inprogress` for deterministic preflight and fresh independent review.
- 2026-09-02 review cycle 1 returned NO-GO because the contract omitted the
  second wrapper's post-admission exact `current == expected` comparison after
  case-insensitive admission. Rescue 1 adds the exact evaluation order,
  identity-drift invalid outcome and connected class-spelling/casing RED/GREEN
  row without changing the five-path/301-LOC decision or product scope.
- 2026-09-02T01:47:56Z publish finalized card into `4.done`; exact ledger retained in ignored manifest.

# 72. Phase 3 — read_only build-out (manager-fixture-v1 result-contract)

## Status
4.done

## Owner
unassigned

## OpenSpec Stage
story

## Source
- 2026-06-13 breadth pass: started Phase 3 (`read_only`, 107 members)
- `docs/protocol-research/phase3-readonly-plan.md`

## Summary
`read_only` is a different acceptance class from Phases 1–2: most members are local
manager reads with no manager→client wire opcode, so the capture/probe pipeline does
not apply. Acceptance runs through the **manager-fixture-v1 result-contract** subsystem
(same-action comparison: Python manager reads the dedicated fixture live; result preview
must contain an expected marker). Runtime is healthy; this session promoted 2 seeds to
reviewed and surfaced the build-out work. Remaining: 7 seeds + 98 uncovered.

**Update 2026-06-13 (batches C→N):** the build-out is largely DONE — **read_only is now
87 accepted_reviewed / 1 seed / 19 uncovered** (overall 118/160). +57 members promoted
across 12 batches via the inline-harness token/handler approach (extended the shared
element-preview block + added `form_state`/`window_state`/`app_state` command_kinds; the
seeds were all promoted along the way). All runs green, 108 tests pass, evidence under
`docs/protocol-research/evidence/readonly-corpus/tm-v1-ro-batch{C..N}-2026-06-13/`.
**Remaining 19 = 15 Decoration (BLOCKED) + 4 hard** — see plan doc "Current state". The
Decoration block needs a USER config edit (add a marked decoration element to the
`ФикстураПротоколаTestClient` form in vanessa_client), then a decoration branch in the
harness. The 4 hard (WaitForCondition / GetUserMessageTexts / GetChoiceListPresentation /
Field.GetObject) need a callback fn / a shown message panel / a choice-list field / are
architecturally N/A on a leaf.

**Update 2026-06-13 (batches O→Q — read_only effectively COMPLETE):** the user added the 3
decorations + a combo field + clicking PF_DECORATION_LABEL_LINK now calls `Сообщить`, unblocking
the rest. read_only is now **103 accepted_reviewed / 1 seed / 3 uncovered** (overall 134/160).
Done: 14 Decoration members (via a `decoration_summary` command_kind), GetChoiceListPresentation
(combo), GetUserMessageTexts (via a `window_messages` handler that clicks the link decoration with
`.Нажать()` — the click method is `Нажать`, not `Нажатие` — then reads the message panel).
**Only 3 left, all blocked/N-A → next version of this card:** (1) App.WaitForCondition — DEFERRED
to next version (needs a callback fn); (2) Field.GetObject + (3) Decoration.GetObject —
architecturally N/A (fields/decorations are leaves; `ПолучитьОбъект` raises ObjectNotFound;
confirmed via the Контрагент reference field = child_count=0). Group.GetObject works and is
already accepted (groups have children).

## Acceptance
- Fixture form (`ProtocolFixtureTestClient.Form.Форма`) carries a deterministically
  marked element per type so every read case has a known expected value (no more
  `object not found by marker`).
- read_only runs in per-object sub-batches (harness timeout no longer truncates the set).
- Each `read_only` member: manager-harness `command_kind` handler + catalog entry +
  `acceptance_contract` → live run → report → `promote_member` to `accepted_reviewed`.
- The 7 remaining `accepted_seed` members are promoted to `accepted_reviewed`.
- An evidence policy for reads-without-a-request-frame is decided and recorded
  (recommended: result-contract is sufficient for read_only; request-hash only where a
  read genuinely round-trips).

## Change Set
- none yet

## Verify
- not started

## Archive
- not started

## Related
- `docs/protocol-research/phase3-readonly-plan.md`
- `docs/protocol-research/evidence/readonly-corpus/tm-v1-ro-full-2026-06-13/`
- `tools/protocol-research/manager_fixture_v1_command_catalog.json`
- `tools/protocol-research/report_manager_fixture_v1.py`
- `docs/protocol-research/evidence/readonly-element-hash-audit/` (prior incomplete_hash work)
- `docs/protocol-research/breadth-roadmap.md`

## Result
not started

## Next
- triage

## Change Plan Notes
When moved to `2.todo/`: 0) **FIRST — fix the inline harness BSL** (the harness is NOT an
EPF/base object; it is inline BSL generated by `tools/protocol-research/run_protocol_capture.ps1`,
run on the manager via `execute_step_from_text` — `invocation_mode=vanessa_mcp_execute_step_from_text_inline_bsl`).
Make it (a) open/activate the fixture form in bootstrap before element/form reads; (b)
return the actual read value in `result_preview` for `active_window`/`active_form`/`*_summary`
(today they return placeholders — object type name `ТестируемоеОкноКлиентскогоПриложения`,
`Нет`); (c) locate the form/elements by the test API's exposed caption (open window shows
synonym `Фикстура протокола TestClient`, not the design title `QA MCP Protocol Fixture V1`)
— and in the fixture обработка `ФикстураПротоколаTestClient` (vanessa_client config) ensure
each target marker is displayed/findable (element reads time out `object not found by
marker`). Catalog edits alone cannot fix this — verified runs `tm-v1-ro-full` /
`tm-v1-ro-form` (2026-06-13). Only diagnostic-dump kinds work (the 2 promoted members).
1) raise/batch the harness timeout, re-run, promote the 7 remaining seeds; 2) decide+record
the reads-without-request evidence policy; 3) per-object sub-batches starting with
TestedFormButton (6).

## Log
- 2026-06-13T05:44:12Z card created

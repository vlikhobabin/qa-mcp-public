# 117. P4 — re-validate all 61 tools on 8.5 + stamp the 8.5 manifest

## Status
4.done

## Order Index
117

## Owner
unassigned

## OpenSpec Stage
story

## Source
- Parent epic 112. Closes the loop: prove the whole tool surface works on 8.5, using the existing
  live-regression harness + the per-capability probe corpus.

## Summary
Point the runtime at 8.5 and re-run the live verification that proved the 8.3 surface, fixing any red via the
build→run-live→fix loop, then version-stamp the 8.5 capture manifest.

## Scope
1. **Live-regression CORE on 8.5:** `PLATFORM_ROOT=…/8.5.x.y python -m qa_mcp.regression` → GREEN (the
   `preflight.protocol_drift` check now sees the 8.5 manifest; the CORE checks drive a live 8.5 client).
2. **Per-capability probe corpus on 8.5:** run the `tools/protocol-research/` per-capability probes against the
   8.5 client; produce a parity report (which of the 61 tools pass on 8.5).
3. **Transpile corpus** — re-measure the Vanessa-corpus transpile metric on 8.5 (should be unaffected — it is
   config/Gherkin, not wire — but confirm).
4. **Re-stamp the 8.5 manifest** — `python -m qa_mcp.regression.versioning --stamp` against the 8.5 set
   (records 8.5.x.y + the 8.5 template hashes).
5. **Fix loop** — recalibrate any environment-coupled check targets (as the original 8.3 harness did), keeping
   the always-restart-Apache + always-teardown discipline.

## Acceptance
- Live-regression CORE GREEN on 8.5 with clean teardown; a per-capability parity report shows the 61 tools'
  status on 8.5; the 8.5 manifest is stamped and the drift detector reports OK against a live 8.5 platform.
- 8.3 live-regression still GREEN (no regression from the multi-version work).

## Dependencies
- P2 (115) + P3 (116) — the working 8.5 asset set (+ any code). P0 (113) — version selection.

## Verify
- Evidence: an 8.5 live-regression GREEN run + the per-capability parity report under `evidence/`.

## Archive
- not started

## Result
- **✅ DELIVERED via validate-first (epic 112).** P4 = the green e2e: the full live-regression
  (`--include-write`) is **7/7 GREEN on a real 8.5 client using the 8.3 bundled data** (read + write + UI→DB) —
  the tool surface exercised through the regression harness, no per-tool re-capture. The 8.5 manifest is stamped
  (commit `f8408ac`). In `main` (303fbbb…2d98c8d).

## Log
- 2026-06-25 created under epic 112.
- 2026-06-29 board hygiene: moved 1.backlog → 4.done (validate-first e2e green satisfied P4).

# 114. P1 — stand up the 8.5 lab capture contour

## Status
4.done

## Order Index
114

## Owner
unassigned

## OpenSpec Stage
artifacts

## Source
- Parent epic 112. Prerequisite for P2 (re-capture). The 8.5 platform (8.5.1.1343) is already installed at
  `/opt/1cv8/x86_64/8.5.1.1343`; an empty 8.5 file IB was created in the probe (`1cv8 CREATEINFOBASE` works).

## Summary
Establish a working 8.5 lab where a genuine TestClient renders the **«QA MCP Protocol Fixture V1»** fixture form
and its wire traffic can be captured — the substrate the P2 re-capture runs against.

## Scope
1. **An 8.5 infobase carrying the fixture form.** Decide + execute one of:
   - convert a **copy** of `vanessa_client` to 8.5 (8.5 opens an 8.3 file IB with a one-time conversion — keep
     the original untouched), or
   - a fresh 8.5 IB + load the «QA MCP Protocol Fixture V1» config/extension that the captures address.
   The fixture form with its fields is required — the templates capture an open-form + value-read against it.
2. **Confirm the 8.5 `/TESTCLIENT` renders the fixture form** under Xvfb (the proven launch recipe, PLATFORM_ROOT
   = 8.5) — login/desktop/fixture-form open, screenshot evidence.
3. **Capture infrastructure on 8.5:** determine the genuine-action capture path —
   - does the Vanessa TestManager (used by `genuine-action-capture-recipe`) run on 8.5? if yes, reuse it;
   - else use the native capture path (`tcpdump` on `lo` over the client TPort range + drive actions). Verify a
     clean pcap of one action is produced on 8.5.
4. **Document the 8.5 lab boot** (an 8.5 analogue of the linux-native-testclient-xvfb recipe).

## Acceptance
- An 8.5 TestClient boots against the 8.5 fixture IB and renders the «QA MCP Protocol Fixture V1» form (evidence).
- One genuine action is captured to a clean pcap on 8.5 and converts via `pcap_to_traffic.py` to `traffic.jsonl`.
- The 8.5 lab boot is documented for repeatable capture runs.

## Change Set
1. `8-5-fixture-lab-contour` — stand up the converted 8.5 fixture IB + prove the TestClient boots/renders + document the lab boot. (`openspec/changes/8-5-fixture-lab-contour/`)
2. `8-5-genuine-action-capture` — determine the 8.5 capture path + capture one genuine action → `traffic.jsonl`. (`openspec/changes/8-5-genuine-action-capture/`)

## Change 1: `8-5-fixture-lab-contour`

### Why
The empty `_bundled/8.5/` slot from P0 needs a working 8.5 lab to re-capture against. Stand up the substrate first.

### Scope
- Convert a COPY of `vanessa_client` to 8.5 (one-time `DESIGNER /UpdateDBCfg`; original untouched) → `/opt/1c-dev/vanessa_client_85/`.
- Boot 8.5 `/TESTCLIENT` under Xvfb against the copy; verify it listens, license OK, renders the managed UI (no Apache stop — separate file).
- Document the 8.5 lab-boot recipe (8.5 analogue of `linux-native-testclient-xvfb`).

### Acceptance — ✅ DONE (2026-06-25)
- 8.5 TestClient boots + renders the converted fixture config (screenshot). **GATE PROVEN** — listening on :15381 in ~3s, empty `testclient.out`, demo desktop rendered.
- 8.5 lab boot documented for repeatable runs (`docs/capture-refresh-runbook.md` → "Adding a new platform version").

### Depends On
- none (P0 architecture already landed)

### Related
- `openspec/changes/8-5-fixture-lab-contour/`
- evidence: `docs/protocol-research/evidence/card114-8-5-lab-gate-2026-06-25/` (gitignored)

## Change 2: `8-5-genuine-action-capture`

### Why
P2's re-capture needs a proven capture path on 8.5; the Vanessa-on-8.5 vs native question must be answered with a first real capture.

### Scope
- Determine the 8.5 capture path (Vanessa TestManager on 8.5 vs native `tcpdump` on `lo` over the client TPort range).
- Open the `lTestClient` fixture form on 8.5, drive ONE genuine action, capture a clean pcap.
- Convert via `tools/protocol-research/pcap_to_traffic.py` → a sample 8.5 `traffic.jsonl`.

### Acceptance — ✅ DONE (2026-06-25)
- One genuine 8.5 action captured to a clean pcap and converted to a parseable `traffic.jsonl`. **DONE** — 8.5 equivalent of `genuine-card98-listform-read` (connect→open Товары list→read «Наименование», `run_scenario`=Success); 45 records, **parses with `CaptureBootstrap.load`** (24 mgr + 21 cli).
- Capture path recorded for P2: Vanessa TestManager on 8.5 (real `HOME`) + `tcpdump -i lo 'tcp portrange 47000-49000'`, client TPort `:48001`.

### Depends On
- `8-5-fixture-lab-contour`

### Related
- `openspec/changes/8-5-genuine-action-capture/`
- evidence: `docs/protocol-research/evidence/card115-8-5-genuine-capture-2026-06-25/` (gitignored)

### Notes
- The earlier «license-gated» finding was a **`HOME`-override bug** (per-user license not found when `HOME` pointed at a scratch dir), corrected; the Vanessa TestManager runs on 8.5 as `lihv` with the real `HOME`.

## Dependencies
- 8.5 platform installed (✓). The fixture config/extension source. Capture tooling (`tools/protocol-research/`).

## Verify
- A screenshot of the rendered fixture form on 8.5 + a sample `traffic.jsonl` from a captured 8.5 action.

## Archive
- not started

## Result
- **✅ P1 COMPLETE — both changes done.** Change 1 (`8-5-fixture-lab-contour`): the 8.5 lab contour is up (converted copy of `vanessa_client` boots an 8.5 `/TESTCLIENT` under Xvfb, demo desktop rendered; lab-boot runbook written). Change 2 (`8-5-genuine-action-capture`): the genuine-action capture pipeline is **PROVEN end-to-end on 8.5** — the Vanessa TestManager on 8.5 (real `HOME`; the «license blocker» was a `HOME`-override bug) drove an 8.5 client through the card-98 list-form read; `tcpdump` → `pcap_to_traffic.py` → a 45-record `traffic.jsonl` that **parses with the production loader**. Both changes `validate --strict` green; offline suite 497 green. (License-free bonus from the same session: first 8.5 handshake divergence pinpointed — `{1,<dec>}`→`{2,<hexGUID>}`.)

## Next
- Ready to publish / archive (P1 done). **P2 (card 115)** scales THIS exact capture path to the full 8.5 corpus (handshake + frame templates + the 13 action/foreground captures + accepted_mappings), bundled into `_bundled/8.5/` via the P0 resolvers. P2's decode also answers P3 (code-vs-data): the handshake envelope/GUID moved, so expect some code.

## Log
- 2026-06-25 created under epic 112. Parallelizable with P0; gates P2.
- 2026-06-25 `$opsx-ff`: decomposed into `8-5-fixture-lab-contour` + `8-5-genuine-action-capture`; both changes apply-ready (validate --strict green). Moved 1.backlog → 2.todo.
- 2026-06-25 `$opsx-do` (Change 1): 8.5 lab gate proven live (convert copy → boot `/TESTCLIENT` → desktop render); lab-boot runbook written.
- 2026-06-25 **CORRECTION (operator caught it):** the «Change 2 license blocker» was wrong — a `HOME`-override bug, not a real licensing limit. The 1C license is per-user under `$HOME/.1cv8/…`; my `/TestManager` attempts overrode `HOME` to a scratch dir → «Не найдена лицензия». Re-run as `lihv` with the real `HOME`: Vanessa TestManager boots on 8.5 (MCP up ~3s, no error). **Change 2 is UNBLOCKED.** Corrected the runbook + change artifacts.
- 2026-06-25 `$opsx-do` (Change 2): genuine 8.5 capture **DONE** — Vanessa TestManager on 8.5 (27 tools) drove an 8.5 client through the card-98 list-form read (`run_scenario`=Success); `tcpdump` (client `:48001`) → `pcap_to_traffic.py` → 45-record `traffic.jsonl` that parses with `CaptureBootstrap.load`. **P1 COMPLETE** (both changes done, validate green, suite 497). Lab torn down. Ready to archive/publish.

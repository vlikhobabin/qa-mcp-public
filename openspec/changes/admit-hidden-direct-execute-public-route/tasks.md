## 1. Exact Baseline And Hostile Oracles

- [ ] 1.1 Materialize a clean composition from published
  `312e91453026cf808e6d48b521c971eb11e7ef96`, record every S1-S6 source/test
  hash and establish an additions-only `<=500` production LOC counter that
  excludes the dirty combined I1 payload.
- [ ] 1.2 Add bridge RED tests for absent/incomplete capability, relative or
  non-EPF/ERF paths, malformed/non-lowercase marker, unknown/conflicting input,
  malformed nested outcome and stale/foreign lifecycle stop; assert zero
  process, desktop, action and stop callbacks on rejection.
- [ ] 1.3 Add Python/MCP RED tests for missing/incompatible capability,
  target/session/generation drift, malformed/replayed S6 receipt, changed or
  duplicate cleanup lease, callback failure, physical schema controls and every
  chooser/global-input/foreground/desktop-switch fallback.

## 2. Bounded Standalone Bridge Integration

- [ ] 2.1 Add one S7-owned adapter that composes published S1/S2 lifecycle,
  S3/S4 observation, optional S5-R1 addressed prompt action and S6 receipt
  binding without modifying or copying certified foundation files.
- [ ] 2.2 Advertise `testclient-hidden-desktop-direct-execute` only with the
  complete adapter and extend the existing authenticated launch request with
  the closed absolute-path/lowercase-marker pair and typed receipt response.
- [ ] 2.3 Bind direct-execute status/stop to the unchanged bridge-owned PID,
  native port, lifecycle id, immutable handle and S6 binding; return only
  bounded refusal or `stopped`/`already_stopped` outcomes.

## 3. Project-Bound Python And MCP Admission

- [ ] 3.1 Extend the bridge client/operation core to negotiate the exact
  capability, derive physical target fields from current application context
  and independently validate the complete S6 receipt/current-run identity.
- [ ] 3.2 Replace the pre-stable `open_external_processor` schema with logical
  EPF/ERF path plus optional expected caption, remove model-visible physical
  controls and condition stable profile admission on a compatible project-
  bound bridge.
- [ ] 3.3 Map only exact admitted receipts and cleanup outcomes to bounded
  public stages, remove private path/UI/handle/error content and retain existing
  Linux behavior without widening its input, evidence or cleanup authority.

## 4. Offline And Clean-Composition Verification

- [ ] 4.1 Run focused hostile/GREEN and full Go tests, `go vet ./...`, Windows
  test/host cross-builds and standalone capability/launch/stop contract tests.
- [ ] 4.2 Run focused Python receipt/operation/profile/schema tests, compileall
  and the exact non-live full Python suite with coverage/report evidence.
- [ ] 4.3 Repeat the clean composition twice and prove `<=500` physical added
  production lines, byte-identical S1-S6 predecessors, authorization relation,
  no non-S7 callers/files and no chooser/OCR/coordinate/global-input/
  foreground/desktop-switch fallback.

## 5. Exact-Source Windows Certification And Recovery

- [ ] 5.1 Run the trusted Windows runtime preflight for the authorized host,
  exact platform `8.3.27.2214`, declared `vanessa_client`, final-source bridge
  executable/Python artifact and owned cleanup contour before any live action.
- [ ] 5.2 Prove final-source prompt-off success and one Python-to-MCP invocation
  with exact capability, receipt, marker, form post-state and single-use cleanup.
- [ ] 5.3 Build two distinct fresh per-run EPFs and prove prompt-on plus recovery
  rerun with one addressed action each, exact S4 post-state, zero `Default`
  leaks/global input/desktop switches and no raw UI retention.
- [ ] 5.4 Prove capability-absent, malformed receipt, stale/replayed identity,
  callback-failure and repeat-stop refusals on final artifacts, then restore
  exact security configuration and remove only current-run task/stage/PID/job/
  desktop/TPort resources while preserving unrelated Docker/listener/reboot
  state.

## 6. Evidence And Delivery Handoff

- [ ] 6.1 Retain an ignored privacy-safe S7 evidence index with exact commands,
  candidate hashes, typed outcomes, counts, before/after inventories and
  recovery receipts; record that no new protocol frames/captures or raw UI were
  required.
- [ ] 6.2 Run strict change/all OpenSpec validation, manifest working-tree and
  staged scope checks, untracked whitespace validation and `git diff --check`,
  then sync only the two S7 capabilities and archive only this change.
- [ ] 6.3 Move the delivered card to its authorization-bound
  `3.inprogress` path and stop for a fresh independent critical review; do not
  admit the stable profile, publish, start OSS-07 or use this task as a `GO`.

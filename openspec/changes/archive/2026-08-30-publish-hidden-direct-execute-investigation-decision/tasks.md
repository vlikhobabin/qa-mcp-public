## 1. Decision Lineage

- [x] 1.1 Audit the retained OSS-06-I1 final source, Windows executable, wheel, platform, target, prompt-off/on/recovery and cleanup hashes and record only sanitized references.
- [x] 1.2 Record chooser `PARTIAL` lineage separately from the eligible hidden direct-`/Execute` decision and forbid diagnostic visible-desktop evidence from admission.

## 2. Non-Admitting Publication Boundary

- [x] 2.1 Finalize the decision/card/spec text with the exact S7 `Blocks` relation and `300`/`500` bounded-successor contract.
- [x] 2.2 Build a delivery manifest containing only this card/change/spec decision and exclude all production payload paths plus `.codex/config.toml` as preexisting dirty work.

## 3. Verification And Evidence

- [x] 3.1 Retain an ignored evidence index mapping each decision claim to the existing exact Windows-native receipt; a live rerun is not applicable because no runtime/source behavior changes.
- [x] 3.2 Run strict change/all OpenSpec validation, explicit untracked whitespace validation, manifest validate/scope-check and deterministic review preflight with `added_production_loc=0`.
- [x] 3.3 Archive/sync the decision capability and prepare its exact payload for one fresh independent critical reviewer before scoped publication.

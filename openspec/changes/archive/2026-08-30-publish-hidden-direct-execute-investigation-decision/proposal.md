## Why

The retained OSS-06-I1 engineering work proves a hidden Windows
direct-`/Execute` architecture, but its combined `1982`-line production payload
cannot enter ChangeRail semantic review. ChangeRail requires a clean published
investigation before any exact successor may receive a bounded authorization.

## What Changes

- Publish a privacy-safe, non-admitting decision bound to the retained native
  source/executable/platform/target/prompt/cleanup lineage.
- Record the chooser route as non-admitting and hidden direct `/Execute` as the
  only eligible successor direction.
- Require sequential production successors under the canonical `300` limit,
  with one separately authorized final integration capped at `500`.
- Leave production code, runtime behavior, stable tool profile and authority
  unchanged.

## Capabilities

### New Capabilities

- `qa-mcp-hidden-direct-execute-investigation-decision`: Defines the published,
  exact-lineage decision and its non-admitting bounded-successor handoff.

### Modified Capabilities

- None.

## Impact

- Affects OpenSpec/card documentation and ignored evidence references only.
- Requires no live rerun, production source change, MCP registration or Windows
  host mutation.
- Existing retained native evidence is audited but not regenerated by this
  documentation-only change.

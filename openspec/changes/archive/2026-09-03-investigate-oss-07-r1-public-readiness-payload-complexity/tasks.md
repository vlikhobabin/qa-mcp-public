## 1. Record The Bounded Decision

- [x] 1.1 Record the exact OSS-07-R1 successor id/path and the retained
  approximately 313-line preflight trigger without importing or certifying the
  successor implementation payload.
- [x] 1.2 Set the maximum future `production_loc_ceiling` to `350`, set
  `allow_new_authority_or_wire_protocol` to `false`, and record the exact
  six-field future authorization object.
- [x] 1.3 Reject whitespace deletion, production-source reclassification, and
  owned-scope exclusion as complexity-gate workarounds; record split or new
  investigation as the fail-closed path above the ceiling.

## 2. Preserve Metadata-Only Boundaries

- [x] 2.1 Preserve exact SPDX `Apache-2.0`, prove OSS-07-I2 remains
  byte-identical and OSS-07-I1 remains absent, and make no OSS-08 or OSS-09
  implementation change.
- [x] 2.2 Sync only the new complexity-decision capability and update only this
  card/change/archive metadata; do not create the later authorization source or
  change/copy/reclassify/exclude any successor implementation path.
- [x] 2.3 Record test-first and Windows-native verification as not applicable
  and prohibited for this metadata-only change; run no Windows, SSH, 1C,
  TestClient, live, Apache-service, network, credential, mutation, or release
  operation.

## 3. Verify And Archive

- [x] 3.1 Audit the exact investigation and successor paths/ids, ceiling `350`,
  authority flag `false`, `Blocks` relation, separate future authorization
  requirement, and prohibited-workaround language.
- [x] 3.2 Prove the working-tree/manifest scope is limited to this card,
  OpenSpec change, synced capability, and archive metadata; prove no successor,
  OSS-07-I2, license, OSS-08, OSS-09, product, test, fixture, evidence-payload,
  or runtime path changed.
- [x] 3.3 Run strict change/capability/all OpenSpec validation, the project
  public-surface scanner when available (otherwise record its absence),
  manifest scope reconciliation, `git diff --check`, and an explicit untracked-
  file trailing-whitespace check.
- [x] 3.4 Archive the completed change, retain the card in `3.inprogress`, and
  prepare the payload for fresh independent ordinary-risk review without
  staging, committing, or publishing it.

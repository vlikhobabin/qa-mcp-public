## 1. Bind The Omission Decision

- [x] 1.1 Create bounded public-safe I16 decision/findings evidence that binds
  published I15, its one canary/no-receipt/zero-candidate outcome, exact cleanup
  and absence of retry or certification authority.
- [x] 1.2 Record omission only from the stable standalone profile/public
  support matrix and prove no product, test, fixture, tracked EPF, runtime
  authority, source profile membership or license path changes; distinguish
  the unchanged pre-stable runtime catalog from stable support admission.

## 2. Reconcile Public And Board State

- [x] 2.1 Update the public README/tool support matrix to identify
  `open_external_processor` as dormant and omitted from stable support pending
  a separately reviewed future qualification.
- [x] 2.2 Reconcile the OSS-06 parent, S4-R1 parent, I13, S7, published I15 card
  and roadmap with actual post-I15 paths, states and the nine-entry evidence
  count while preserving incomplete lineage.
- [x] 2.3 Correct OSS-07 to a `1.backlog` story with no OpenSpec artifacts,
  record it as the next separate card after publication and carry the
  operator-approved `Apache-2.0` handoff without planning or implementation.

## 3. Offline Verification

- [x] 3.1 Validate decision JSON and exact board/dependency assertions,
  including published I15, active/unarchived I13, incomplete S4-R1/S7 and the
  absence of OSS-07 active/archive change artifacts.
- [x] 3.2 Run strict change/capability/all OpenSpec, scoped public-surface and
  privacy checks, Apache-2.0 path checks, tracked-plus-untracked whitespace and
  delivery-manifest working-tree scope reconciliation.
- [x] 3.3 Record Windows-native/live verification as not applicable: this
  decision changes no executable behavior and the card explicitly forbids SSH,
  historical-user, Windows, 1C, `/Execute`, canary, S4/S5/S7 and candidate actions.

## 4. Sync Archive And Review Handoff

- [x] 4.1 Sync the decision capability into main specs, archive only this
  completed change and keep I13 plus unrelated active changes unarchived.
- [x] 4.2 Leave the card in `3.inprogress`, update its verification/result/log
  and manifest summaries, then pass deterministic preflight and fresh
  independent ordinary-risk review before scoped publication.

## 1. compose-display-settings

- [x] 1.1 Recheck the admitted source baseline and constructor/guard/error call sites against design.md; preserve unrelated work and stopped FIX-02 behavior.
- [x] 1.2 Add C1/C3 regressions through real application factories with conflicting explicit settings and env, including local mode and intentionally empty remote settings. Observe fake HTTP requests; prohibit real HTTP, X11, sockets and signals.
- [x] 1.3 Add Settings-owned backend construction and route composed display selection, availability and related configuration diagnostics through it. Preserve existing pin, timeout, window and native-port semantics; keep explicit legacy env adapters.
- [x] 1.4 Run the affected display/factory nodes with durations and retained results; distinguish initial failing regressions from successful checks of the changed source.

## 2. route-host-lifecycle-settings

- [x] 2.1 Route bound/unbound launch, status and stop and supported unbound attach consumers through the same application configuration. Bind the current admitted attachment per operation without shared mutable backend targets.
- [x] 2.2 Cover C2 with distinct fake lifecycle identities, attachment replacement, owned positive controls and stale/foreign rejection. Keep unproven bound non-owned attach blocked before any backend/native call; stopping A must not signal or clear B.
- [x] 2.3 Cover C4 nested, interleaved async and exceptional context exit, plus explicit legacy env controls. Assert that routing never mutates process environment and C3 public errors exclude synthetic credentials/private values.
- [x] 2.4 Run affected lifecycle/context nodes and reuse unchanged successful evidence from checkpoint 1. Broaden checks only for modified consumers, failures or a concrete uncovered branch.

## 3. verify-and-document

- [x] 3.1 Update docs/shared-core-extension.md with explicit Settings ownership, legacy compatibility and the boundary between offline routing proof and FIX-11/12 native qualification. Synchronize the accepted runtime-configuration delta through the native delivery workflow.
- [x] 3.2 Inspect the affected-module test plan against the admitted baseline, run relevant offline checks with JUnit/durations, and run integration checks separately only for affected integration consumers. Compile changed Python and validate the scoped change/canonical contract as appropriate. A full pytest run requires epic closure or explicit agreement.
- [x] 3.3 Update card Result/Log with C1–C4 observations, exact commands, counts, durations and residual native qualification; record final focused evidence for the final payload and hand off to the authorized runner for independent review/finalization.

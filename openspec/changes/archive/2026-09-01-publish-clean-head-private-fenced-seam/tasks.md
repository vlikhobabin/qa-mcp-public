## 1. Baseline and core fence

- [x] 1.1 Retain clean-HEAD RED evidence for the absent four seam families and exactly two direct worker inventory calls.
- [x] 1.2 Implement the private closed liveness/inventory/fenced sample and bounded pre-receipt diagnostic in `hidden_direct_execute_observation.go`.
- [x] 1.3 Add injected hostile core tests for live/exited/unknown, error/empty/non-empty, post-change, exact call counts, malformed/private diagnostics and zero retry/fallback/action.

## 2. Windows connection and transferred ownership

- [x] 2.1 Implement exact child/listener process, job and response-TPort fence helpers plus `observeHiddenDirectWindowsFencedSample`.
- [x] 2.2 Replace exactly the two direct inventory calls in `observeHiddenDirectInWorker`, connect both bounded diagnostics and preserve existing fail-closed/passive-UIA results.
- [x] 2.3 Implement transferred-observer validation and the post-acknowledgement callback while the existing worker-local job handle remains held.
- [x] 2.4 Add connected Windows-tree hostile tests for both real worker boundaries, main absent/exact, handle-close failure and exact job/TPort observer ownership.

## 3. Exact-tree verification and evidence

- [x] 3.1 Run focused/full host-agent Go tests and vet plus all repository Linux tests without 1C, endpoint or Windows execution.
- [x] 3.2 Produce deterministic Windows amd64/386 test cross-builds without executing them; record Windows-native execution as prohibited and not run.
- [x] 3.3 Prove the seven-path and production-LOC ceilings, exactly two wrapper calls/no direct bypass, authorized-predicate hunk mapping and explicit staging plan.
- [x] 3.4 Run strict change/all OpenSpec, exact staged-tree compile/tests, manifest scope, diff and untracked-aware whitespace checks and retain curated privacy-safe RED/GREEN evidence.

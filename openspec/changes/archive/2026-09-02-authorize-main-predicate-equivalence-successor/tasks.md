## 1. Bind The Exact Authorization Source

- [x] 1.1 Verify that I10 and its machine decision are unchanged tracked
  published artifacts, that I10 blocks exact I11, and that the decision's
  `authorization_source.payload` is the sole exact six-field source.
- [x] 1.2 Preserve the exact object in I10a and its normative capability with
  ceiling `301`, authority/wire flag `false`, exact I10/I11 identities and no
  additional or missing field.
- [x] 1.3 Leave the S4-R1 parent and I11 byte-for-byte unchanged and record that
  I11 implementation, size, identity and proof-floor admission remain future
  work.

## 2. Sync The Documentation-Only Capability

- [x] 2.1 Sync the main-predicate-equivalence successor authorization
  capability without changing product, test, protocol, provider or runtime
  requirements.
- [x] 2.2 Update only I10a card/change/archive metadata required for the exact
  source and preserve Apache-2.0 unchanged.

## 3. Verify And Archive

- [x] 3.1 Audit exact JSON bytes, six keys, exact dependency/block identities,
  exact authorization-time I11 path and the independent-publication gate.
- [x] 3.2 Prove the payload is documentation/OpenSpec-only, with no host-agent,
  I11, parent, runtime-evidence or external-operation byte; protocol evidence
  generation is not applicable because no protocol knowledge changes.
- [x] 3.3 Run strict change/capability/all OpenSpec validation, docs-only scope,
  public-surface/privacy, Apache-2.0 and tracked-plus-untracked whitespace
  gates.
- [x] 3.4 Record Windows-native/test-first/live verification as prohibited and
  not applicable, archive the completed change and retain I10a in
  `3.inprogress` for fresh independent ordinary/high review.

## Why

R5-R1 replaced the exhausted runtime patch staircase with a viable immutable
positive-schema design and closed its original denylist defect, but its final
review found conflicting normative public outcomes for one declared text
mismatch. Its rescue budget is exhausted, so a clean linked replacement must
re-materialize the accepted design from published `main` and make that outcome
unique before runtime implementation resumes.

## What Changes

- Retain exact R5 and R5-R1 failed lineage, review and stash evidence without
  restoring either payload.
- Re-materialize context-local evidence authority, exact artifact/JSON bounds,
  immutable operation/path-local literal admission and the complete hostile,
  URL and safe-control matrices.
- Separate outcomes unambiguously: undeclared keys omit; every declared-path
  class, exact-type, value or subclass mismatch returns the complete fixed
  `invalid-executor-result`.
- Recreate A4 → R7 → A5 → R8 and keep OSS-04E blocked until reviewed R8
  publishes.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `qa-mcp-operation-boundary-concurrency-bounds-design`: extend the published
  design with exact failed lineage, positive literal/artifact admission,
  deterministic mismatch outcomes and the replacement successor chain.

## Impact

Documentation, OpenSpec and board metadata only. No Python/runtime/test code,
protocol behavior, provider authority, wire format or dependency changes. No
Windows, Docker, TestClient, live 1C or other external action is required.

## 1. Preserve the pre-investigation boundary

- [x] 1.1 Record the empty staged index, blocked S4-R1 payload fingerprint,
  four immutable S4-R2 hashes, and protected S7/fixture/OSS-07/OSS-08
  fingerprints in ignored card-owned evidence.
- [x] 1.2 Prove the one allowed historical-user endpoint is reachable and that the exact
  task/stage names selected for this card do not preexist.

## 2. Build the disposable authority probe

- [x] 2.1 Create the exact session worker and outer task runner under ignored
  evidence with privacy-safe typed statuses and bounded exit-code fallback.
- [x] 2.2 Run PowerShell syntax and structural proofs showing snapshot-first,
  `finally` restoration as the sole final probe operation, no later probe read,
  the exact endpoint, and absence of 1C/S3/S4/S5/S7/action surfaces.

## 3. Execute the Windows-native matrix

- [x] 3.1 Run the same owner/worker chain as a session-0 disposable control and
  exercise
  fail-closed token, snapshot/read, metadata-set, and evidence-write status
  mappings without retaining raw values.
- [x] 3.2 Run one exact `InteractiveToken`/`Limited` session-1 disposable proof
  on `historical-user@192.0.2.201`, retain typed privacy-safe evidence, and clean only
  the exact task, stage, probe, and stage-bound process state.

## 4. Materialize the non-admitting handoff

- [x] 4.1 Publish a durable evidence summary that classifies the missing
  receipt boundary, records the disposable session results, and selects
  session-0 ownership for any future real-target snapshot/restoration.
- [x] 4.2 Recheck zero 1C/action/live-row execution, all preservation
  fingerprints, exact empty staging, strict change/all OpenSpec validation,
  manifest scope, privacy scan, and `git diff --check` before review handoff.

## 1. Bound Schemas And Operations

- [x] 1.0 Retain adversarial RED for model-visible physical/evidence inputs and disconnected read/display executor routes before implementation.
- [x] 1.1 Compose project-bound wrappers that hide physical target, endpoint, credential, rebind and evidence-retention inputs while preserving unbound schemas.
- [x] 1.2 Route representative safe read and display through the common executor with current target/session and bounded artifact provenance.
- [x] 1.3 Confirm added production LOC is at most `300` and no OSS-05/OSS-06 behavior enters the payload.

## 2. Exact-Owned Cleanup

- [x] 2.0 Retain adversarial RED for stale, foreign, recycled and non-owned handles, relay/native-port separation and zero alternate-target/downstream signals before implementation.
- [x] 2.1 Require current target/session/generation plus exact lifecycle/process handle for local and Windows cleanup.
- [x] 2.2 Detach non-owned attachment without signalling and refuse stale/foreign/recycled handles.
- [x] 2.3 Cover relay/native-port separation and prove failures never create or select an alternate infobase/direct-Xvfb target.

## 3. Native Runtime Certification

- [x] 3.1 Audit final source hashes against prior lineage evidence and classify the expected bounded-sequence mismatch before execution; do not restore either exhausted stash.
- [x] 3.2 Run exact-final-source Linux target-bound lifecycle/read/display/cleanup proof with Apache and pre-existing process restoration.
- [x] 3.3 Run exact-final-wheel authorized Windows-native lifecycle/read/display/cleanup proof with exact task/PID/listener/stage/tunnel ownership and unrelated Docker/1C preservation.
- [x] 3.4 Keep credentials, connections, raw screenshots, UI text, binaries and large logs ignored; retain sanitized hashes/verdicts/inventory only.

## 4. Delivery Gate

- [x] 4.1 Run focused core/MCP/scenario/lifecycle/display tests, compilation, host-agent Go tests, exact non-live CI/coverage, diff check, strict OpenSpec validation and the production LOC gate `<=300`.
- [x] 4.2 Sync/archive and prepare the deterministic critical preflight plus fresh independent review handoff. Review, publish, parent OSS-04 closure and OSS-06 unblocking remain card-level gates.

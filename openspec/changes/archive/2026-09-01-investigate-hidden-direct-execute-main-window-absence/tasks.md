## 1. Exact Baseline And Hostile Diagnostic Contract

- [x] 1.1 Bind published S1-R1 through S6, blocked S7 production/test hashes,
  tracked `run-1.epf`, authorized platform/target/principal and current clean
  runtime inventory; fail on substitution or dirty manifest scope.
- [x] 1.2 Add RED tests for production caller/import drift, raw path/UI/
  credential retention, unbounded samples, enumeration error treated as
  success, missing process exit and incomplete cleanup receipts.
- [x] 1.3 Define one bounded test-only timeline schema covering monotonic stage,
  PID/job membership, listener/process exit, desktop hash, top-level count,
  allowlisted class/owner relation and cleanup state.

## 2. S3/S4 Exact-Source Diagnostics

- [x] 2.1 Build Windows test diagnostics from a clean exact composition and
  prove published predecessors plus blocked S7 production bytes are unchanged
  and no production caller references the diagnostic surface.
- [x] 2.2 Run the real published S3 control and record its exact argv,
  environment hash and bounded process/desktop/window timeline without raw UI.
- [x] 2.3 Run two fresh tracked-EPF S4 diagnostics through child exit or admitted
  main identity, recording transient enumeration events separately from the
  final `main_not_ready` or later stage.
- [x] 2.4 Repeat the classification with the uninstrumented exact published S4
  path so diagnostic timing cannot satisfy acceptance by itself.

## 3. Root-Cause Decision And Cleanup

- [x] 3.1 Compare S3/S4 timelines and classify process exit, `/Execute` loading,
  desktop/window identity, security/startup state or remaining uncertainty with
  concrete evidence for each resolved or explicitly unresolved hypothesis.
- [x] 3.2 Publish exactly one continuation decision: byte-preserving S7 resume,
  bounded linked replacement/authorization scope, or `NOT-VERIFIABLE` with an
  exact external-state resume condition.
- [x] 3.3 Remove only current-run scheduled task, stage, PIDs/job/desktop/TPort
  and restore exact configuration; prove zero owned residue and preservation of
  unrelated protected task/listener/Docker/boot state.

## 4. Verification And Review Handoff

- [x] 4.1 Run focused diagnostic schema/hostile tests, full Go tests/vet,
  Windows cross-builds, predecessor/S7 byte-identity and no-production-caller
  scans.
- [x] 4.2 Retain an ignored privacy-safe evidence index with exact candidate
  hashes, bounded timelines, decision and cleanup receipts; retain no raw UI,
  credentials, screenshots or protocol captures.
- [x] 4.3 Run strict change/all OpenSpec validation, manifest scope, untracked
  whitespace and `git diff --check`.
- [x] 4.4 Sync/archive the exact decision for fresh independent high-risk review;
  resume S7 or create the authorized successor exactly as decided.

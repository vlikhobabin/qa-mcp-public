## 1. Reconfirm Lineage And Exact Scope

- [x] 1.1 Verify the clean implementation base is exact published reference
  `10598ef201fd5afe4b0a9fdf110a6e0fb7bace24`, and reconcile the card's I1
  fingerprint, verdict/history digests, cycle-3 acceptance outcomes, single R1
  blocker and rescue budget `2/2` without resolving or inventing a predecessor
  runtime path.
- [x] 1.2 Prove the I1 nine-path payload and `audit_design.py` are absent from
  the clean baseline, then record that no I1 bytes will be restored, copied or
  reconstructed and that only the five design-declared successor paths are
  authorized.
- [x] 1.3 Record the inherited `Apache-2.0`, structured-context allowlist,
  offline verification and successor-only scope constraints; fail closed on
  production/test, baseline OSS-07/roadmap, OSS-08, Windows, SSH, 1C,
  TestClient, live MCP, Apache service or network work.

## 2. Author The Frozen Clean-Room Matrix

- [x] 2.1 Create the successor `matrix.json` with deterministic UTF-8/LF bytes,
  the exact ordered row identities, exact per-row `P`/`S` semantics, exact
  results, literal aggregate `[46, 14, 5, "D14"]`, and the safe synthetic G18
  fixture `eligible@example.invalid` with exact hex, length and SHA-256.
- [x] 2.2 Create the successor-specific `audit_design.py` that accepts only the
  five documented structured-context keys, emits row-bound structured results,
  emits D12 evidence only from the executed callback, and emits pre-rule G18
  byte receipts for every exact non-email rule.
- [x] 2.3 Compute exact matrix/helper byte sizes and lowercase SHA-256 digests,
  write canonical `freeze.json`, and create `verify_matrix.py` with the exact
  freeze-record digest, independent ordered row/label/result oracle and literal
  `46/14/5/D14` expectation.

## 3. Prove False-Pass Resistance

- [x] 3.1 Make the unchanged control pass only after semantic, byte, context,
  D12, G18 and aggregate/per-row checks all succeed; parse both JSON files and
  compile both Python helpers offline.
- [x] 3.2 Run isolated owned-temporary mutations for a `P`/`S` relabel; each of
  `46`, `14`, `5` and `D14`; D12 bypass with marker retained; changed and
  omitted per-rule G18 bytes/receipts; a helper-byte edit; row add/remove/
  duplicate/reorder; and every invalid structured-context class. Require each
  hostile case to return its named non-zero failure and remove only the owned
  temporary directory.
- [x] 3.3 Write `findings.md` with exact source hashes, control outcome,
  per-mutation expected/observed failure classes and closure mapping for the
  three formerly unbacked claims. Retain no raw logs, private data, runtime
  paths or positive claim beyond the exact synthetic offline matrix.

## 4. Run The Inherited Offline Gate

- [x] 4.1 Reconcile the final diff against the exact card/OpenSpec plus five
  successor-path allowlist, confirm no production/test/baseline-card/OSS-08
  changes, and scan the successor surface for secrets, personal/customer data,
  private endpoints and any license choice other than `Apache-2.0`.
- [x] 4.2 Run the exact successor verifier with mutation mode, JSON parsing,
  Python compilation, strict change validation, strict all-content validation,
  `git diff --check`, and an equivalent whitespace check over all untracked
  files through a temporary Git index without staging the real index.
- [x] 4.3 Record Windows-native verification as not applicable and do not run
  it: this card is explicitly Linux-offline design evidence with no production,
  test, runtime or platform behavior. Reconfirm no SSH/1C/live/network action
  and retain the exact command/outcome summary for fresh review.
- [x] 4.4 Sync and archive only after every task and acceptance is evidenced,
  then request a fresh independent ChangeRail review at `critical` risk against
  the exact payload. Do not reuse I1's negative verdict as positive evidence or
  start OSS-08.

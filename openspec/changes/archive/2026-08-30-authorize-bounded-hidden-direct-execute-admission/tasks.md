## 1. Published Investigation Precondition

- [x] 1.1 Verify OSS-06-I2 is an unchanged tracked `openspec/board/4.done/` artifact and its `Blocks` relation names exact successor S7.
- [x] 1.2 Verify S7 `Depends On` includes exact investigation id and its published-authorization reference names this card's eventual canonical `4.done` path/id.

## 2. Exact Authorization Contract

- [x] 2.1 Finalize the six-field inline authorization source with exact investigation/successor paths and ids, ceiling `500` and protocol allowance `true`.
- [x] 2.2 Prove mismatch fixtures for stale lane/path/id/relation, another successor, ceiling above `500` and absent protocol allowance fail deterministic preflight.
- [x] 2.3 Build a documentation-only manifest excluding every production successor path and `.codex/config.toml`.

## 3. Verification And Publication Handoff

- [x] 3.1 Audit the retained Windows-native decision reference without rerunning Windows because the authorization changes no runtime behavior.
- [x] 3.2 Run strict change/all OpenSpec, explicit untracked whitespace, manifest validate/scope-check and deterministic preflight with zero production LOC.
- [x] 3.3 Archive/sync the authorization capability and hand it to a fresh independent critical reviewer before scoped publication.

## Why

The final hidden direct-execute integration introduces one backward-compatible
bridge capability and may contain more than the ordinary `300` production LOC.
Deterministic preflight accepts that boundary only from a clean published
authorization tied to an exact published investigation and exact successor.

## What Changes

- Publish one authorization source for
  `oss-06-s7-admit-hidden-direct-execute-public-route`.
- Set the exact production ceiling to `500` and allow the declared
  backward-compatible wire capability for that successor only.
- Keep every foundation successor at the ordinary `300` limit.
- Make no production, runtime, profile or authority change in this card.

## Capabilities

### New Capabilities

- `qa-mcp-hidden-direct-execute-admission-authorization`: Defines the exact
  investigation/successor binding, ceiling and fail-closed mismatch behavior.

### Modified Capabilities

- None.

## Impact

- Affects OpenSpec/card authorization documentation only.
- Depends on the investigation decision being published and tracked in
  `4.done` before delivery validation.
- Does not execute Windows 1C, change host-agent/Python source or admit the
  public tool.

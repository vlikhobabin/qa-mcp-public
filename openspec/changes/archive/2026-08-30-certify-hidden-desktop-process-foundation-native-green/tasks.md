## 1. Replacement Lineage And Exact Source

- [x] 1.1 Validate the exhausted S1 cycle-3 verdict/history, final blocker,
  `2/2` rescue budget and exact four-file source/test hashes without rewriting
  predecessor evidence.
- [x] 1.2 Compose the replacement candidate from published HEAD plus only the
  four exact adopted S1 files; prove `248/300` production LOC, dormancy and no
  wire/global-input/S2 scope.

## 2. Exact Windows GREEN Certification

- [x] 2.1 Run focused/full Go plus Windows test and host cross-builds and bind
  the replacement candidate SHA-256 to the exact adopted source hashes.
- [x] 2.2 Execute the literal Windows selector for ProcessContract,
  CleanupClosesHandlesWhenTerminateFails and NativeLifecycle in one exact-owned
  task and retain sanitized pass evidence only.
- [x] 2.3 Verify exact task/action/arguments/path/hash ownership before cleanup,
  remove only the current-run task/stage and prove listener `18081`, Docker,
  unrelated processes, Defender and reboot state were preserved.

## 3. Evidence, Spec And Review Handoff

- [x] 3.1 Create a replacement evidence index whose argv and oracle claims
  exactly match executed commands; cite predecessor RED as immutable lineage
  rather than manufacturing a new RED.
- [x] 3.2 Sync the terminate-failure cleanup scenario, archive the completed
  replacement change and update the card/delivery manifest.
- [x] 3.3 Run strict OpenSpec, manifest scope-check, canonical LOC preflight,
  `git diff --check` and prepare the exact payload for one fresh independent
  replacement-card review before publication.

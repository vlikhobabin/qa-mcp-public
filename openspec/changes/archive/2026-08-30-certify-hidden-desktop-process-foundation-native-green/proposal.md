## Why

The exhausted S1 payload is otherwise accepted, but its final retained Windows
evidence claimed a terminate-failure cleanup oracle that the literal native
test selector did not execute. A linked replacement must make that behavior
explicit and obtain exact-source GREEN proof without consuming a forbidden
third same-card rescue.

## What Changes

- Adopt the exact dormant four-file S1 source/test payload without changing its
  production behavior or activating a caller.
- Make the terminate-failure dual-handle-close outcome explicit in the
  foundation capability.
- Run one hash-bound Windows candidate with a literal selector that executes
  ProcessContract, CleanupClosesHandlesWhenTerminateFails and NativeLifecycle.
- Replace the overclaimed retained native summary with command-exact sanitized
  evidence and exact-owned cleanup proof.

## Capabilities

### New Capabilities

- none

### Modified Capabilities

- `qa-mcp-hidden-desktop-process-foundation`: Explicitly require both owned
  handle-close attempts and joined errors when termination itself fails during
  exact current-call cleanup.

## Impact

The replacement touches the four existing S1 internal Go source/test files only
as adopted unpublished lineage, the S1 capability spec, OpenSpec/card workflow
documents and ignored sanitized verification evidence. It does not touch
protocol tools, Python manager code, MCP provider setup or runtime lab config;
it requires no live 1C, Vanessa MCP, EDT/meta snapshot or protocol capture.
Windows-native offline test execution is the only external verification.

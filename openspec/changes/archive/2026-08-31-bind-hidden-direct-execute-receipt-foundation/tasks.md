## 1. Scope And Hostile RED

- [x] 1.1 Record the exact published baseline, staged-zero state, preexisting
  dirty exclusions and hashes for all published S1-S5 source/test files.
- [x] 1.2 Add isolated Go RED tests for malformed nested/envelope schema,
  status, hash and counts; stale/foreign cleanup identity; post-bind mutation;
  shared-ledger replay and repeated stop, asserting zero rejected callbacks.
- [x] 1.3 Add equivalent isolated Python RED tests including exact-type
  rejection (`bool` versus `int`), unknown/missing fields and failed-cleanup
  replay, asserting zero rejected cleanup/action/public calls.
- [x] 1.4 Retain privacy-safe RED commands and typed outcomes without raw UI,
  paths, credentials, handles, screenshots or exception content.

## 2. Go Typed Receipt And Cleanup Lease

- [x] 2.1 Add the portable internal S6 envelope over validated S2/S4 and
  optional S5 receipts with distinct prompt-free/prompt-confirmed statuses.
- [x] 2.2 Bind exact run/token/desktop hashes, port and worker/child/listener
  PIDs to one canonical lowercase SHA-256; export no handle or raw identity.
- [x] 2.3 Add exact admission, canonical mutation fingerprint and caller-owned
  consumed-ledger behavior that consumes before one injected cleanup callback.

## 3. Python Typed Receipt And Cleanup Lease

- [x] 3.1 Add frozen internal Python cleanup/receipt types and strict parser for
  the exact Go envelope and nested published successful outcomes.
- [x] 3.2 Recompute and compare current-run cleanup binding independently,
  fingerprint the exact envelope and consume a caller-owned ledger before one
  callback with no retry after callback failure.
- [x] 3.3 Keep the module dormant: add no package export, non-test caller,
  public capability/tool/profile route or S7 integration.

## 4. Offline Verification And Clean Composition

- [x] 4.1 Run exact focused Go/Python GREEN tests, full `go test ./...`, full
  Python suite, `go vet ./...`, Windows test cross-build and host cross-build.
- [x] 4.2 Compose published `3a0e0f6...` in a fresh module-aware tree plus only
  exact S6 source/tests and rerun focused/full Go/Python and build gates.
- [x] 4.3 Prove added production LOC `<=300`, published S1-S5 source/test hashes
  unchanged, no non-test callers and no public/forbidden/privacy escape.

## 5. Delivery Handoff

- [x] 5.1 Retain an ignored privacy-safe S6 evidence index with command,
  hash/count and typed outcome summaries; no Windows live rerun is applicable
  because certified S5 behavior/artifact boundaries remain unchanged.
- [x] 5.2 Sync only `qa-mcp-hidden-direct-execute-receipt-foundation`, archive
  only this change and move only S6 to review-gated `3.inprogress`.
- [x] 5.3 Run strict change/all OpenSpec, delivery-manifest working-tree scope,
  staged-zero and `git diff --check`; stop ready for fresh independent review
  without launching review, committing, pushing or starting S7/OSS-07.

## 6. Review Cycle 1 Rescue

- [x] 6.1 Add hostile cross-language PID boundary tests proving Go `uint32`
  maximum acceptance and `4294967296` rejection in Python with zero callbacks;
  retain the failing pre-fix result.
- [x] 6.2 Add concurrent shared-ledger replay tests in Go and Python asserting
  exactly one cleanup callback; retain the pre-fix Go race and Python double-
  callback results.
- [x] 6.3 Bound every Python PID to `1..4294967295` and make shared-ledger
  admission/consumption atomic in both languages without changing the wire
  schema, public routes or predecessor files.
- [x] 6.4 Re-run focused race/Python, full Go/vet/Python, Windows cross-build,
  clean composition, LOC, predecessor, caller/privacy/forbidden, strict
  OpenSpec, manifest scope and diff gates for fresh review cycle 2.

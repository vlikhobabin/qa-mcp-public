## 1. Bind Exact Investigation Preconditions

- [x] 1.1 Reconfirm all five published-I11 blob ids and the zero path diff
  from `964e29f`; record that no product/test path is dirty.
- [x] 1.2 Connect only through the exact operator-supplied SSH authority and
  prove the exact hostname,
  principal, session, platform, target, candidate, fixtures and I13 argv hashes.
- [x] 1.3 Capture bounded configuration bytes/ACL/metadata hashes and hashed
  unrelated-task topology before-images; require zero pre-existing I14-owned
  task, stage or process state.

## 2. Isolate The Two I13 Stops

- [x] 2.1 Perform static published-source analysis and one bounded I14 S4
  investigation attempt, retaining only closed failure class/stage, hashes,
  counts, booleans, exit code and duration; do not start another I13 row.
- [x] 2.2 Compare privacy-safe hashed unrelated-task sets and correlate only a
  bounded Task Scheduler event window; determine harness behavior, independent
  drift or ownership violation without touching foreign tasks, or retain
  `not_verifiable` without inference when attribution is unavailable.
- [x] 2.3 Restore exact configuration state, remove only I14-owned task, stage,
  process/job/desktop and transport state, then repeat each evidenced cleanup
  and record both proven zero residue and any unprovable continuity/zero-state.

## 3. Retain Decision And Verification Evidence

- [x] 3.1 Write bounded typed decision/findings artifacts that classify every
  finding as contour, harness, published behavior, independent system drift or
  ownership violation, or as `not_verifiable` when evidence cannot distinguish
  them; explicitly preserve I13 as uncertified and name any separately
  authorized successor.
- [x] 3.2 Validate retained JSON, privacy/secret exclusions, exact five-blob and
  no-product/test-diff gates, focused S4-R2 Go tests, full Go tests/vet and the
  tracked-plus-untracked whitespace surface.
- [x] 3.3 Run strict change/capability/all OpenSpec, delivery-manifest scope and
  public-surface/license checks; sync and archive only the completed I14
  investigation capability, leaving I13 active and unarchived.
- [x] 3.4 Prepare the completed payload for fresh independent critical/xhigh
  review and permit publication only after a valid fresh `go`; perform no
  substantive edit after review.

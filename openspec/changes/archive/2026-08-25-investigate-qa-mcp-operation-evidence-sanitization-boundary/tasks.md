## 1. Materialize The Investigation

- [x] 1.1 Create the durable operation-evidence boundary investigation document with exact OSS-04D/R1 lineage, final findings, root causes and invariant decisions.
- [x] 1.2 Record the typed serializer, structured field/URL admission, artifact/error policy, route admission and adversarial verification matrix without modifying production code.

## 2. Create The Successor Handoff

- [x] 2.1 Create the exact bounded authorization-source card tied to this investigation, one named implementation successor, a 500-line ceiling and no new authority or wire protocol.
- [x] 2.2 Create the ordered typed-boundary implementation successor card with test-first floors, exact-source Windows offline proof and fresh independent review requirements.
- [x] 2.3 Update the original OSS-04D card, parent epic and OSS-04E dependency so the authorization and implementation successors are the only sequential continuation.

## 3. Verify And Finalize The Design Payload

- [x] 3.1 Retain a secret-free evidence index with exact verdict/fingerprint hashes, decision/matrix checks and named stash inventory; verify each stash's actual reconstructed tree without claiming the older checkpoint is the final OSS-04D payload.
- [x] 3.2 Verify all finding-to-decision mappings, successor ids/paths, reciprocal authorization fields, documentation links and absence of `src/` or runtime implementation paths.
- [x] 3.3 Record Windows-native verification as not applicable: this design-only payload changes no Windows/runtime behavior and performs no SSH, TestClient, host-agent or Docker action.
- [x] 3.4 Run strict change/all OpenSpec validation and diff checks, sync the design capability, archive the change and prepare the exact payload for fresh independent design review.

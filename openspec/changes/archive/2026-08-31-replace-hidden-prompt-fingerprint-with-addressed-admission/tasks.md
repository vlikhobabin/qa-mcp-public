## 1. Replacement Lineage And Hostile RED

- [x] 1.1 Bind the replacement manifest to published `81c60ef...`, the exact
  four isolated S5-R1 source/test paths and the exhausted S5 evidence lineage;
  record published S1-S4 hashes before editing.
- [x] 1.2 Add portable RED cases where complete observations contain fewer,
  more or changed unrelated controls and changed whole-topology diagnostics
  while the exact prompt/action tuple remains unique; prove the old `26/5/0`
  and fixed-pattern gates reject these safe cases.
- [x] 1.3 Add hostile RED cases for missing/old/duplicate/foreign prompt,
  incomplete enumeration, missing/duplicate/foreign/disabled/changed action,
  pre-action prompt/action drift and used ledger; assert zero focus/key/action
  calls for every rejection.
- [x] 1.4 Add RED privacy/diagnostic cases for command failure, malformed rows,
  foreign PID rows and ambiguous action matches, proving only allowlisted typed
  codes, bounded counts, booleans and hashes cross the observer boundary.
- [x] 1.5 Retain exact RED commands, observed failures and mappings to removed
  false-negative gates or preserved unsafe-action defenses before production
  edits.

## 2. Minimal Portable Admission Policy

- [x] 2.1 Remove fixed total control/Invoke/Value and fixed whole-prompt hash
  predicates from portable admission and receipt validation while keeping them
  as diagnostic-only fields.
- [x] 2.2 Admit only one exact new prompt plus one unique target action whose
  PID, type, path, enabled/visible/invokable state, local selector and dynamic
  root/action geometry are valid.
- [x] 2.3 Require two consecutive equal prompt/action samples, using the second
  as immediate pre-action re-admission while allowing non-target inventory
  diagnostics to drift.
- [x] 2.4 Preserve zero-to-one ledger ordering, injected action-after-admission,
  prompt closure, no replacement prompt and exact passive S4 post-state without
  retry.

## 3. Windows Observer And Addressed Action

- [x] 3.1 Make the UIA traversal bounded and complete-or-fail, enumerate target
  candidates independently of total row counts and reject traversal/row-read
  failures that could hide a duplicate target.
- [x] 3.2 Return sanitized cause-level diagnostics for runtime/command stage,
  enumeration completion, observed/exact-PID row counts, target match count and
  hash validity without raw UI, coordinates, identifiers or exception text.
- [x] 3.3 Re-observe and bind the exact target immediately before the existing
  hidden-local focus plus exact-prompt Return key-down/key-up pair; retain zero
  global input, mouse, foreground or desktop-switch APIs.
- [x] 3.4 Keep the replacement dormant with no non-test caller and no Python,
  MCP, public route, wire/profile, S6 or S7 integration.

## 4. Offline And Clean-Composition Verification

- [x] 4.1 Run exact focused S5-R1 tests, full `go test ./...`, `go vet ./...`,
  Windows test executable cross-build and Windows host-agent cross-build.
- [x] 4.2 Reconstruct published `81c60ef...:host-agent` twice in fresh
  module-aware directories, overlay only the four exact S5-R1 paths and retain
  deterministic test/host candidate hashes.
- [x] 4.3 Prove production additions `<=300` physical lines, all published
  S1-S4 files byte-identical, no non-test callers and no forbidden global/
  visible/public actions; run `git diff --check`.

## 5. Exact Windows Prompt, Contour And Recovery Proof

- [x] 5.1 Before mutation, record listener `18081` state as diagnostic-only and
  verify trusted `HISTORICAL-LAB-HOST\\historical-user`, platform `8.3.27.2214`,
  target `C:\\1C_BASES\\vanessa_client`, exact current-run task/stage and
  Docker/boot/config pre-state without retaining secrets or raw UI.
- [x] 5.2 Build two distinct fresh per-run comment-only EPFs, run the exact-
  source prompt-on candidate and retain typed proof of one exact admitted
  action, two addressed messages, prompt closure, exact S4 post-state and zero
  global input/desktop switches/operator-desktop windows.
- [x] 5.3 Restore exact security-config pre-state, perform the fresh recovery
  rerun and remove only current-run task/stage/PIDs/job/desktop/TPort resources;
  prove Docker/boot are unchanged and retain listener state only as a bounded
  non-gating diagnostic.
- [x] 5.4 Prove S5 has no connection, restart, stop or reconfiguration path for
  listener `18081`; do not gate native proof on its presence, ownership or
  independent drift.

## 6. Delivery Handoff

- [x] 6.1 Create an ignored S5-R1 evidence index covering hostile RED/GREEN,
  clean composition, native prompt/recovery, contour integrity and cleanup with
  no raw captures, UI text, credentials, connection strings or screenshots.
- [x] 6.2 Sync only `qa-mcp-minimal-addressed-prompt-admission`, archive only
  this replacement change and update only S5/S5-R1/S6 lineage plus the scoped
  delivery manifest; exclude roadmap publication, historical combined payload,
  S6/S7 implementation and `.codex/config.toml`.
- [ ] 6.3 Run strict change/all OpenSpec, manifest working-tree/staged scope,
  LOC, predecessor, caller, forbidden-action, privacy and diff gates; obtain one
  fresh independent ordinary/high `GO` before scoped publication.

## 7. Bounded Review Rescue 1

- [x] 7.1 Retain review-cycle-1 `NO-GO`, then isolate the fingerprint RED with
  the inherited three-sample prerequisite satisfied and a temporary legacy
  `26/5/0` plus whole-pattern mutant that fails specifically at that gate.
- [x] 7.2 Make archived proposal/design consistent with the authorized listener
  re-scope: unknown ownership and independent `18081` drift are diagnostic-only,
  non-gating and never operated on by S5.
- [x] 7.3 Rerun focused/full Go, vet, deterministic clean composition, exact-
  source Windows proof with two new fresh EPFs, exact cleanup, strict OpenSpec,
  manifest/diff gates and deterministic preflight before fresh review cycle 2.

## 1. Establish The Closed Evidence Boundary

- [x] 1.1 Verify safe baseline/origin, published I7/I8 identities and the
  rejected/unpublished I4/I9 lineage without modifying any old workspace.
- [x] 1.2 Bind I9 conclusions only to canonical cycle-1, cycle history,
  evidence index, concise retained outputs and cycle-3 reviewed tree
  `cda26645...`; exclude the contaminated later working tree.
- [x] 1.3 Enumerate first/second expected predicates, row validation,
  membership/match order, empty inventory and handle/cardinality outcomes from
  published source and the reviewed I9 Git object.

## 2. Publish The Investigation Decision

- [x] 2.1 Write a curated findings report with exact predicate/provenance,
  outcome, call/control and connected RED/GREEN matrices.
- [x] 2.2 Write and parse a machine-readable bounded decision that records the
  baseline, exact five paths, `301` ceiling, no authority/wire allowance,
  required separate authorization and exact I11 identity.
- [x] 2.3 Decide quantitative sufficiency versus I8 identity authority and
  prepare a separate exact authorization-source card plus apply-ready I11
  successor only if every design condition is supported.

## 3. Preserve Roadmap And Scope

- [x] 3.1 Update the S4-R1 parent with rejected/unpublished I4/I9 findings,
  I10 result and exact next step while keeping it blocked and unimplemented.
- [x] 3.2 Prove the payload changes no host-agent product/test path, preserves
  Apache-2.0 and contains no public API/route/wire/runtime authority.
- [x] 3.3 Record Windows-native/test-first/live verification as prohibited and
  not applicable; run no Windows, PowerShell, 1C, endpoint, lab/live/action,
  S5/S7 or SSH operation.

## 4. Sync Verify And Archive

- [x] 4.1 Sync the decision capability to main specs and validate the change,
  capability and all OpenSpec artifacts strictly.
- [x] 4.2 Run exact decision-schema/value, lineage, successor relation,
  docs/OpenSpec-only scope, parent-blocked, public-surface, license and
  tracked-plus-untracked whitespace gates with named outcomes.
- [x] 4.3 Archive the completed change, update the delivery manifest and card,
  and leave I10 in `3.inprogress` for deterministic preflight and fresh
  independent ordinary/high review.

## Why

Published I14 could not admit a typed Session-1 canary on the original
interactive route, so it accepted no S4 candidate result and left the
historical started-route topology delta unattributed. The operator has now
separately authorized one fail-closed admission canary and, only after its
success, one exact-source S4 isolation on that same contour.

## What Changes

- Reconfirm the unchanged published-I11 five-blob lineage and exact platform,
  target, candidate, run-1 fixture, configuration and argv identities.
- Require a successful typed Session-1 canary before invoking the candidate;
  otherwise retain `NOT-VERIFIABLE`/`BLOCKED` and stop without substitution or
  retry.
- After successful admission only, run at most one exact-source S4 isolation
  and retain a closed privacy-safe result.
- Observe bounded scheduler/topology attribution over the original started
  route, then restore the exact configuration and double-clean exact-owned
  state.
- Publish evidence/docs/OpenSpec/board state only. No protocol tool, Python
  manager, MCP setup, runtime lab configuration, product/test source, public or
  wire behavior, or license path changes are authorized.

## Capabilities

### New Capabilities

- `qa-mcp-i15-session1-admitted-s4-isolation`: Defines the fail-closed
  admission, one-attempt isolation, bounded topology-attribution and exact
  cleanup decision gate for I15.

### Modified Capabilities

- None.

## Impact

Delivery requires the exact separately authorized Windows/1C contour and may
create only exact-owned temporary task, stage, process/job/desktop and
transport state. The reviewed payload is limited to bounded public-safe
evidence, OpenSpec artifacts and card/roadmap summaries. It changes no product
or test byte, adds no authority or behavior, does not retry the I13 matrix and
does not certify I13.

## 1. Bind Exact Preconditions

- [x] 1.1 Reconfirm the five published-I11 blob ids, zero path diff from
  `964e29f`, clean product/test paths and exact unchanged candidate; bind the
  published run-1 expected hash and record that its conditional runtime gate
  was not reached after canary failure.
- [x] 1.2 Connect only through the freshly operator-authorized original contour
  and prove exact host/principal/controller session, interactive Session 1,
  platform, target and configuration identities without retaining raw private
  values; keep runtime fixture/S4 argv unclaimed because admission failed.
- [x] 1.3 Retain initial configuration content/ACL/creation/write/access/
  attributes before-images and typed zero-state for exact task/stage/process/
  job/desktop/transport plus 1C processes; retain canary protected counts and
  the exact-normalized post-cleanup hash, and classify the missing pre-start
  identity hash as `not_verifiable`.

## 2. Gate And Run The One Authorized Isolation

- [x] 2.1 Stage only exact-owned bounded controller material and run one typed
  Session-1 canary on the original started route; validate host/principal/
  session predicates and candidate invocation count zero.
- [x] 2.2 If and only if task 2.1 succeeds, run one exact-source tracked run-1
  S4 isolation with a typed pre-invocation marker and retain only closed
  class/stage, hashes, counts, booleans, receipt presence, exit code and
  duration. Otherwise record `NOT-VERIFIABLE`/`BLOCKED`, invoke no candidate
  and stop the live branch.
- [x] 2.3 Compare available protected topology/count evidence through the
  original started route and exact-normalized post-cleanup hash plus a bounded
  scheduler-event projection; distinguish
  harness/start behavior from external drift only when evidenced, otherwise
  retain `not_verifiable` without inference and never target foreign state.

## 3. Restore And Retain The Decision

- [x] 3.1 Restore the exact configuration before-image with last-access as the
  final operation, remove only exact-owned task/stage/process/job/desktop/
  transport state, repeat cleanup and prove zero work plus zero owned/1C
  residue on the second pass.
- [x] 3.2 Write bounded schema-valid decision/findings and a typed evidence
  index; retain no raw output, UI, screenshot, credential, connection string,
  broad dump or task name, preserve I13 active/unarchived/uncertified and
  update affected parent/roadmap summaries truthfully.
- [x] 3.3 Validate JSON/evidence privacy and consistency, repeat exact source
  lineage and configuration/cleanup gates and stop publication on any foreign
  target, residual state or unproven required before-image.

## 4. Offline Verification Review And Archive

- [x] 4.1 Run focused S4/S4-R2 Go tests, `go test ./...` and `go vet ./...` in
  `host-agent/windows-display-agent`, plus deterministic unexecuted Windows
  amd64/386 cross-build pairs against unchanged product/test bytes.
- [x] 4.2 Run strict change/capability/all OpenSpec, delivery-manifest scope,
  public-surface/privacy, Apache-2.0 and tracked-plus-untracked whitespace
  gates; record commands and observed outcomes in the manifest.
- [x] 4.3 Sync and archive only the completed I15 decision capability, leave
  I13 active and unarchived, and prepare the complete payload for fresh
  independent critical/xhigh review before scoped publication.

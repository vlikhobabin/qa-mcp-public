## 1. Freeze The Exact Decision

- [x] 1.1 Record the exact OSS-05 successor id/path, API major `1`, allowed route family and explicit excluded private routes.
- [x] 1.2 Record the minimum ChangeRail machine ceiling `301`, OSS-05 delivery cap `300` and exact-successor-only wire authorization.

## 2. Prove Metadata Scope

- [x] 2.1 Confirm the investigation changes only OpenSpec/board metadata and performs no runtime, Windows, live 1C, Docker or external action.
- [x] 2.2 Prove the investigation blocks only OSS-05 and cannot authorize OSS-06 or another successor.

## 3. Verify And Publish

- [x] 3.1 Run exact decision/relation checks, strict change/capability/all OpenSpec validation, manifest scope and diff checks.
- [x] 3.2 Sync/archive and prepare fresh independent review before publishing the bounded investigation source.

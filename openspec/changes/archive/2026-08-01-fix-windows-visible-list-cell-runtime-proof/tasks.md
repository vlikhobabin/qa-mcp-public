## 1. Windows RED And Diagnosis

- [x] 1.1 Run the authorized Windows preflight before any UI action: verify the expected host identity, active interactive session, exact prior task/stage absence, and source-bound build inputs; retain only sanitized booleans and hashes.
- [x] 1.2 Add a Windows-only Go regression that creates an owned empty-title target plus a visible named native descendant and invokes the production visible-cell reader from a separate execution thread; run it against the current reader and retain a genuine failing RED marker-presence outcome.
- [x] 1.3 Compare only bounded UIA view/apartment/fixture signals needed to classify the failure, retaining marker-presence booleans, counts, allowlisted type categories, and cleanup state without raw names, handles, titles, process ids, screenshots, response bodies, or logs.

## 2. Narrow Reader Or Fixture Fix

- [x] 2.1 Apply the smallest proven UIA reader/execution or fixture-contract correction that returns the owned marker without changing lifecycle/PID/TPort validation, desktop-session gating, selector precedence, or weak-target refusal.
- [x] 2.2 Keep the rescue payload separable from the dirty S50-120 predecessor: record exact card-owned paths/hunks and add no Python MCP, protocol, 1C source/metadata, infobase, or business-data change.

## 3. Verification And Evidence

- [x] 3.1 Re-run the same Windows-native regression GREEN and record why it observes the production reader boundary and would fail again if named descendants disappear.
- [x] 3.2 Run `go test -count=1 ./...` and `GOOS=windows GOARCH=amd64 go test -c` in `host-agent/windows-display-agent`; retain command outcomes and source hashes.
- [x] 3.3 Run the full source-bound S50-120 Windows screenshot/key/type/click/visible-cells/explicit-selector/unrelated-foreground/weak-target/typed-session matrix against the exact owned target and retain only sanitized evidence under `.runtime/changerail/evidence/fix-windows-visible-list-cell-runtime-proof/`.
- [x] 3.4 Perform exact cleanup of the run-owned task, stage, processes, screenshots, token, and logs; audit retained JSON keys and record that no infobase, business data, native TestClient protocol, or unrelated Windows resource was accessed or mutated.
- [x] 3.5 Run `openspec validate fix-windows-visible-list-cell-runtime-proof --strict`, `openspec validate --all --strict`, `git diff --check`, and an explicit whitespace scan for untracked card-owned files.

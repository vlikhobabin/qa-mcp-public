## 1. Record The Bounded Decision

- [x] 1.1 Confirm the effective ChangeRail parser's exact investigation,
  authorization-source, successor-reference and reciprocal-link fields against
  current tracked/local contract sources and published examples.
- [x] 1.2 Record exact I4 as the only conditionally eligible bounded successor,
  with an at-most-five-path/301-LOC ceiling, closed authority flag, mandatory
  future hostile verification floor and explicit architectural stop conditions.
- [x] 1.3 Record the exact future I8 authorization object and I4 two-field
  reference without creating I8 or changing/copying retained I4.

## 2. Sync The Decision Capability

- [x] 2.1 Sync the new private classifier authorization decision capability to
  main OpenSpec specs without changing existing product/runtime requirements.
- [x] 2.2 Update only card/change/archive metadata required for the
  investigation handoff and keep Apache-2.0 unchanged.

## 3. Verify And Archive

- [x] 3.1 Audit exact ids, canonical paths, ceiling `301`, authority flag
  `false`, `Blocks` relation and separate-authorization requirement.
- [x] 3.2 Prove the diff is docs/OpenSpec-only and imports no retained I4
  product, test, fixture or runtime-evidence bytes; record Windows-native/live
  verification as prohibited and not applicable to this decision payload.
- [x] 3.3 Run strict change/capability/all OpenSpec validation, explicit scope,
  privacy/forbidden-surface and untracked-aware whitespace checks.
- [x] 3.4 Archive the completed change, retain the card in `3.inprogress` and
  prepare fresh independent review without staging, committing or publishing.

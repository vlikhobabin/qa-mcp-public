## 1. Portable Disposable Infobase

- [x] 1.1 Export a portable backup from the retained extension-bearing Linux
  disposable infobase and record successful platform output under ignored
  evidence; test-first RED is not applicable to this runtime transfer step.
- [x] 1.2 Verify `.200` hostname and clean exact S50-161 namespace, stage only
  run-owned files, create the Windows disposable infobase, restore the backup,
  and retain bounded before/restore inventory.

## 2. Owned TestClient And UI Proof

- [x] 2.1 Launch the restored infobase through the source-built host agent and
  retain a live owned PID, TPort and lifecycle handle without a consuming local
  readiness probe.
- [x] 2.2 Open `Справочник.S50ProofItems`, create one unique disposable item,
  read it back from the visible catalog list/cells, and retain ignored
  screenshot evidence using qa-mcp lifecycle-bound tools.

## 3. Recovery And Rerun Evidence

- [x] 3.1 Stop only the owned lifecycle, close only run-owned tunnels/ports,
  unregister exact run-owned tasks, stop exact host-agent/TestClient processes,
  and remove the exact stage after resolving each target.
- [x] 3.2 Retain post-cleanup and second clean-preflight inventories proving no
  exact stage, task, process or proof listener remains and no unrelated Docker
  resource or user data was touched.

## 4. Verification And Handoff

- [x] 4.1 Run focused/full affected tests, Windows cross-build, strict OpenSpec
  validation, public/secret scan and diff check; record commands and outcomes.
- [x] 4.2 Summarize the supported Windows-created/restored path, ignored evidence
  locations, mutation recovery/rerun proof and root S50 tracker handoff in the
  card and delivery manifest.

## Evidence

- Portable source: `s50-161-deployed-proof.dt`, SHA-256
  `bae9a4efd849c0a478a4920d8835710dd7946b21a528ea17f930d71a5053f752`;
  ignored `linux-dumpib.log` records a successful platform dump.
- Final Windows run:
  `.runtime/changerail/evidence/s50-161-fix-windows-disposable-infobase-testclient-launch/20260801T193000Z/`.
  Restore exit is zero, launch owns its PID/TPort/lifecycle handle, visible cells
  contain `S50 proof item 20260801T193000Z` and code `000000001`, and six ignored
  screenshots cover link navigation, empty list, create form, saved item and
  final readback.
- `07_cleanup_actions.json`, `08_after_inventory.json` and
  `09_rerun_preflight.json` show the exact stage absent with no proof tasks,
  processes or listeners. No Docker resource or unrelated user path was
  inspected or removed.

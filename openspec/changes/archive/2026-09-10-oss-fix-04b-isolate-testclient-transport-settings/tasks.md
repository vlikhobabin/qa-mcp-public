## 1. bind-settings-scope

- [x] 1.1 Add focused desired-behavior regressions for C1/C4 using real factory binding, distinct Settings and conflicting process relay; retain the failing baseline without secrets.
- [x] 1.2 Implement the minimal configuration accessor and bind/reset it inside existing application activation, preserving explicit bound absence and exact restoration after nested sync/async failures and actual thread dispatch.
- [x] 1.3 Verify explicit env compatibility outside composition and active-scope precedence inside composition, unchanged process environment/attachments, and no low-level reverse dependency into core or mcp_server; retain focused checkpoint proof.

## 2. isolate-relay-consumers

- [x] 2.1 Consume scoped relay fields in shared transport, validate partial/malformed configuration before sockets, preserve exact endpoint matching and bound absence, and keep rejection diagnostics free of secrets/peer prose (C1/C2).
- [x] 2.2 Add fake-socket consumer regressions for session/replay, foreground, native write/mutation/XTEST and lifecycle/attachment resolution; assert exact destination, synthetic auth preface, timeout and no bypass of target admission (C1/C3).
- [x] 2.3 Verify matching relay, different direct endpoint, malformed token, refusal/hostile reply cleanup and listener-only readiness with no auth or protocol bytes; retain passing source-bound evidence (C2/C3).

## 3. document-and-verify

- [x] 3.1 Document application scope, legacy adapters and offline/native limits in docs/shared-core-extension.md and docs/qa-mcp-tool-reference.md where relevant; assess every design verification-matrix row.
- [x] 3.2 Run focused affected tests in separate offline/integration lanes, changed-file compilation and strict change/canonical validation; no full suite or live runtime is implied.
- [x] 3.3 Finalize Result/Log and record fresh C1–C4 typed observations with exact selected tests, source/input hashes, before/action/after assertions and current receipts; include all consumer families and controls directly in the mapping.
Runner-owned finalization (after completion of all task groups; not a checkpoint task):
Complete native semantic sync and successful aggregate handoff through the installed
runner contract. Preserve accepted plan sections. Independent review, archive,
final floor, done and publication remain required within the shared allowance.

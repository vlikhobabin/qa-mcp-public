## Why

Rejected I4 and I9 showed that a broad hidden-main admission error cannot be
projected to true absence unless the connected decision proves exact
equivalence with every first- and second-boundary predicate. Published I10 and
I10a now authorize one bounded implementation attempt that preserves the
predecessor call and control behavior while failing closed on invalid or
ambiguous inventory state.

## What Changes

- Reconstruct the five canonical I9 cycle-3 product/test blobs from reviewed
  Git tree `cda26645dc8f0e3cc1bab8761125b0de1af0fb9c`, never from its later
  contaminated working tree.
- Add a private boundary-specific evaluator that replays only admission-
  recorded ordered membership booleans, consumes them exactly and performs no
  membership or Windows operation of its own.
- Connect both real observation decisions to the complete I10 predicate,
  provenance, row-order, exact-identity and disagreement contract so only
  proven absence receives an existing closed success cause.
- Retain connected mutation-sensitive tests for every rule and for exact
  calls, sleeps, outcomes, UIA order, privacy and zero action.

## Capabilities

### New Capabilities

- `qa-mcp-main-predicate-equivalence-enforcement`: Enforces I10's exact private
  first/second hidden-main predicate equivalence and predecessor invariants
  within the separately authorized I11 envelope.

### Modified Capabilities

- None.

## Impact

The payload changes exactly five private Go product/test paths under
`host-agent/windows-display-agent/` and the card-owned OpenSpec workflow
artifacts. It adds no exported API, route, wire field, retry, fallback, wait,
Windows operation, desktop mutation, handle transfer, authority, marker
derivation or public caller. Production additions are capped at 301 lines
against `2517ed6b55a6efed8f2cd84b46f8dea18090fd36`; Apache-2.0 remains
unchanged. Verification is offline on Linux, including unexecuted deterministic
Windows cross-builds. Live 1C, Vanessa MCP, EDT/meta snapshots, Windows or
PowerShell execution, endpoints, labs, actions, S5/S7 and SSH are neither
required nor authorized.

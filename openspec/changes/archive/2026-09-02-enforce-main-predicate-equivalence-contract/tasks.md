## 1. Prove Authorization And Reconstruction

- [x] 1.1 Prove local HEAD and credential-free HTTPS `origin/main` start at
  `7952f06261b907c2d789c1d803e38bbc11a61b11`, I10/I10a are unchanged tracked
  `4.done` sources and the exact six-field I11 authorization graph is valid.
- [x] 1.2 Read only Git tree
  `cda26645dc8f0e3cc1bab8761125b0de1af0fb9c` from the immutable I9 source and
  prove all five blob IDs/SHA-256 values equal published I10.
- [x] 1.3 Reconstruct exactly the five reviewed product/test blobs, record the
  initial lineage and prove no old workspace working-tree byte was used.

## 2. Retain Connected RED Evidence

- [x] 2.1 Run identical first/second connected rows for every expected,
  provenance, inventory-row, order, cardinality and empty-inventory rule and
  retain behavioral RED evidence for each removed guard, including first class
  length and second exact returned-identity equality with class drift.
- [x] 2.2 Retain behavioral RED evidence for disconnected first/second
  projections, live replay, after-UIA projection, replay underflow/surplus,
  invalid cause synthesis and changed first `200 ms` sleep/outcome, binding
  exact calls, sleeps, outcomes, UIA, privacy and zero action.

## 3. Enforce The Complete Equivalence Contract

- [x] 3.1 Implement boundary-specific pure ordered-boolean replay with exact
  consumption, zero replay membership/Windows calls and no retained dynamic
  identity or error data.
- [x] 3.2 Connect admission/replay equivalence at both real boundaries,
  preserving exact/absent/invalid disagreement rules and allowing an existing
  closed cause only for proven absence before passive UIA.
- [x] 3.3 Preserve two successful inventories, all fences, 12 membership
  calls, predecessor polling and `200`/`250 ms` sleeps/outcomes, two UIA calls,
  privacy and zero action; format the five files and record final blobs/LOC.

## 4. Verify Sync And Archive

- [x] 4.1 Retain final GREEN for the identical connected matrix and run focused
  plus full Go tests and `go vet`.
- [x] 4.2 Run repository Linux pytest with development coverage and not-live
  selection plus deterministic unexecuted Windows amd64/386 cross-build pairs.
- [x] 4.3 Run strict change/capability/all OpenSpec and exact authorization,
  scope, blob, LOC, call, privacy, public-surface, license, whitespace,
  evidence and manifest gates; confirm no prohibited external contact.
- [x] 4.4 Sync the enforcement capability, archive the completed change, leave
  the card in `3.inprogress` and obtain a fresh independent ordinary/high GO
  before scoped HTTPS publication.

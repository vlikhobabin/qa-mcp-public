## 1. Delivery Safety And Evidence Inputs

- [x] 1.1 Record zero staged paths and the reproducible supplied S4, S7,
  fixture and OSS-07 baselines before decision edits; before review retain
  current remote-push readiness and the explicit I5/protected ownership
  partition without claiming a historical pre-edit remote receipt.
- [x] 1.2 Inspect only the published I3 artifacts, blocked I4 card/artifacts, privacy-safe retained I4 manifest/composition evidence and bounded local source/diff predicates; record no runtime or endpoint evidence.

## 2. Clean-HEAD Versus Dirty-Candidate Boundary

- [x] 2.1 Enumerate the private fence sample, inventory and pre-receipt diagnostic predicates required by I4, their owning files/call edges and their presence or absence in clean published HEAD.
- [x] 2.2 Map each required dirty-candidate dependency to bounded source predicates and determine whether it is separable from protected S4-R1 behavior without a dormant, partial or uncompilable composition.
- [x] 2.3 Apply the design's five-condition fail-closed rule and select exactly one Outcome A or Outcome B; resolve every open question before publication.

## 3. Curated Decision And Handoff

- [x] 3.1 Write one privacy-safe curated findings report containing the reproducible boundary, evidence ledger and exactly one decision outcome.
- [x] 3.2 For Outcome A, state exact owned paths or source predicates, dependency ceiling, hostile verification floor, rollback and fail-closed staging rule for one later seam card; for Outcome B, require architectural redesign and supersession of I4. Grant no implementation or live authority in either case.
- [x] 3.3 Update only the I5 card and, if strictly necessary, minimum I4 linkage metadata; preserve I4 artifacts and every prohibited payload otherwise unchanged.

## 4. Verification And OpenSpec Completion

- [x] 4.1 Verify focused privacy/forbidden-endpoint rules, untracked-aware
  whitespace, the exact supplied baselines, explicit manifest scope and
  `git diff --check`; confirm zero production/test source paths in I5 scope and
  state the evidence limit for protected files lacking a pre-I5 digest.
- [x] 4.2 Run strict change and all-OpenSpec validation, sync the decision capability, archive the completed change and re-run strict all validation.
- [x] 4.3 Record that Windows-native, 1C, endpoint, target, live and S7 verification is not applicable and prohibited for this documentation/OpenSpec-only decision; run none of those surfaces.

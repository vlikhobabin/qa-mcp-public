## 1. Preflight And Baseline

- [x] 1.1 Confirm the authorized Windows host identity, interactive-session readiness, prior exact-stage cleanup, and source-bound host-agent/native-test/fixture hashes; retain only sanitized booleans and hashes.
- [x] 1.2 Record the prior `type_text` transport failure as RED evidence showing that HTTP status and typed error code were not preserved.

## 2. Bounded Proof Diagnostics

- [x] 2.1 Copy the prior ignored proof into a new exact-name ignored run and instrument its HTTP boundary to retain only integer status, allowlisted typed code, and host-agent/fixture liveness booleans.
- [x] 2.2 Run the bounded source-bound Windows reproduction and classify it as proof-client parsing/transport, `foreground-denied`, typed desktop-session refusal, unexpected owned-process exit, or product invariant failure.

## 3. Branch Outcome And Verification

- [x] 3.1 Complete the applicable branch: for a harness-local defect, repair the ignored harness and produce the full five-route, explicit-override, weak-target, isolation, typed-session, native-test, and cleanup matrix; for a product defect, create a linked implementation card with the exact invariant and the same rerun floor.
- [x] 3.2 Confirm the retained evidence contains only sanitized status classes, typed codes, hashes, booleans, test outcomes, and cleanup state, with no raw response, identity, screenshot, credential, or log material.

## 4. Cleanup And Handoff

- [x] 4.1 Stop only proof-owned processes and remove the exact scheduled task, stage, screenshots, token, and logs; retain sanitized before/after cleanup evidence.
- [x] 4.2 Update this card and the blocked S50-120 handoff with the diagnosis, applicable outcome, evidence paths, and exact resume command.
- [x] 4.3 Run `openspec validate investigate-windows-display-type-transport-proof --strict`, `openspec validate --all --strict`, and whitespace checks over all planning artifacts.

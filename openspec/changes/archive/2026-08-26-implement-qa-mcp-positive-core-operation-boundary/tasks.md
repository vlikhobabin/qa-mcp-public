## 1. Establish Intended Oracles

- [x] 1.1 Add direct RED tests for constructor-proof immutable schemas, exact operation/path rules, undeclared-value non-inspection and the complete six-class valid/wrong-type/subclass/invalid-domain matrix.
- [x] 1.2 Add direct RED tests for context/scope isolation, receipt/root/policy/path forgery, exact artifact grammars/order/accounting and receipt scalar controls.
- [x] 1.3 Add direct RED tests for URL depth/public-host controls, same-path hostile/control cells, depth/item/node/string/final-byte boundaries and malformed-edge totality; retain the exact failing command/output.

## 2. Compose Trusted Core State

- [x] 2.1 Implement sealed callback-free object/array/leaf schema nodes and a private immutable per-application catalog for the exact `read_active_window` paths.
- [x] 2.2 Implement exact sealed logical provenance admission and extend `OperationResult` with safe optional provenance serialization.
- [x] 2.3 Give every `ApplicationContext` its own frozen operation catalog and evidence ledger derived only from immutable runtime-target evidence configuration.

## 3. Enforce Receipt Authority

- [x] 3.1 Implement exact-platform-`Path` ledger roots, active operation scopes, authoritative receipts, strict containment/symlink rechecks and deterministic scope-local cleanup.
- [x] 3.2 Enforce sanitized path omission, full-local exact-scope receipt use and 2,047/2,048/2,049 receipt-scalar outcomes without executor authority.

## 4. Reconstruct Positive Results

- [x] 4.1 Implement closed declared-path reconstruction, the six exact field classes, source-owned non-success errors and one complete `invalid-executor-result` partition.
- [x] 4.2 Implement the four-round fixed-point public HTTP(S) canonicalizer with depth-1–4 benign preservation and private/userinfo/query/fragment/credential/depth-5 rejection.
- [x] 4.3 Implement exact artifact id/media/SHA/sensitivity order and shared depth/item/node/string accounting, followed by the complete 65,536-byte canonical DTO gate.
- [x] 4.4 Contain deleted/mutated/raising/oversized/non-finite/encoding edges in bounded fixed results with no exception or input fragment.

## 5. Publish The Core Surface

- [x] 5.1 Export the schema/provenance/ledger/normalizer primitives and document their use, limits and R8 integration boundary in `docs/shared-core-extension.md`.
- [x] 5.2 Prove current MCP, ScenarioRunner, lifecycle and executor routing remain unchanged and added production LOC is at most 300.

## 6. Verify Runtime-Independent Behavior

- [x] 6.1 Pass the direct boundary suite, focused shared-core/runtime-target suite, exact full non-live suite with coverage, Python compilation and deterministic source checks.
- [x] 6.2 Run exact-source Windows offline boundary proof in an owned staging directory, retaining source hashes, Python/platform identity, pre/post process/listener/staging inventory and owned cleanup.
- [x] 6.3 Pass strict OpenSpec, manifest scope, evidence-index and full-tree whitespace checks; record live 1C/protocol/Docker/external proof as not applicable.

## 7. Finalize Delivery

- [x] 7.1 Sync the new capability, archive the change and update the R7 card/parent roadmap with exact verification evidence.
- [x] 7.2 Prepare the exact archived payload for fresh independent review and publish only on a fresh `GO`.

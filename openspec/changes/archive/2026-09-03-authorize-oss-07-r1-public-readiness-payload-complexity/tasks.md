## 1. Bind The Exact Authorization Source

- [x] 1.1 Verify the exact I1 card is an unchanged tracked `4.done` artifact,
  blocks exact OSS-07-R1 at the prepared authorization-time id/path, and is the
  sole investigation dependency without importing the absent successor.
- [x] 1.2 Preserve the I1-prepared six-field authorization JSON byte-for-byte,
  with integer ceiling `350`, boolean authority/wire flag `false`, and no
  additional or missing field.
- [x] 1.3 Record that the successor must separately depend on I1, reference only
  this exact unchanged tracked `4.done` source, pass deterministic preflight,
  retain full evidence and receive fresh independent semantic review.

## 2. Preserve The Metadata-Only Boundary

- [x] 2.1 Keep the successor implementation and metadata absent from this
  payload and prohibit whitespace deletion, production-source reclassification
  and owned-scope exclusion as complexity-gate workarounds.
- [x] 2.2 Sync only the public-readiness complexity-authorization capability and
  preserve exact SPDX `Apache-2.0`, published OSS-07-I2, absent OSS-07-I1 and
  the OSS-08/OSS-09 exclusion.

## 3. Verify And Archive

- [x] 3.1 Audit exact JSON keys/order/values, exact I1 dependency and successor
  binding, and fail-closed later-admission conditions.
- [x] 3.2 Reconcile the delivery manifest to docs/OpenSpec metadata only and
  prove no implementation, test, fixture, evidence payload, license, runtime,
  service or unrelated active-change path entered scope.
- [x] 3.3 Run strict change/capability/all OpenSpec validation, tracked and
  untracked whitespace checks, and focused public-safety/license/lineage scans.
- [x] 3.4 Record test-first, live, SSH, Windows-native, 1C, TestClient,
  Apache-service, credential, mutation, network and release verification as
  prohibited and not applicable; archive the completed change and retain the
  card in `3.inprogress` for fresh independent review.

## 1. Offline Investigation

- [x] 1.1 Audit the retained first-inventory blocker, same-candidate receipt
  and disposable-probe conclusion without invoking any runtime surface.
- [x] 1.2 Audit the tracked child/listener transfer, inventory and historical
  investigation predicates and record the exact evidence limits.

## 2. Bounded Decision

- [x] 2.1 Publish the privacy-safe findings with exclusive child, listener,
  inventory-empty, inventory-error, non-empty/main and post-fence states.
- [x] 2.2 Bind exactly one later fenced-sample correction, one hostile offline
  verification target and the no-second-correction stop condition.
- [x] 2.3 Record that Windows-native/live verification is not applicable to
  this docs/spec-only offline decision; assign all Windows execution and exact
  cleanup verification to a separately resumed S4-R1 implementation session.

## 3. Verification And Handoff

- [x] 3.1 Prove the exact protected blocked S4-R1 and foreign
  S7/OSS-07/OSS-08 paths remain unchanged during the review-cycle-1 rescue by
  comparing them with the immutable independently reviewed tree; reconcile the
  explicit card-owned delivery manifest. No delivery-start digest is claimed.
- [x] 3.2 Retain current strict all-OpenSpec validation, focused documentation
  privacy, untracked-aware scoped-tree whitespace, manifest-scope and
  `git diff --check` evidence. The unavailable pre-archive strict-change output
  and absent public-surface scanner are not claimed.
- [x] 3.3 Sync the capability specification, archive the completed change and
  leave the card in progress for a fresh independent ordinary-risk review.

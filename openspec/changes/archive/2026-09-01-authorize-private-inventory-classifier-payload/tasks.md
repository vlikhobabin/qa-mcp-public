## 1. Bind The Exact Authorization Source

- [x] 1.1 Verify the exact I7 card is an unchanged tracked `4.done` artifact,
  depends on published predecessors as recorded, and blocks exact I4 at the
  prepared authorization-time id/path without reading or changing I4.
- [x] 1.2 Preserve the exact six-field authorization JSON and exact sole I7
  dependency in I8, with ceiling `301`, authority/wire flag `false` and no
  additional or missing field.
- [x] 1.3 Record explicitly that I4 path/LOC/blob/hostile verification remains
  future admission work and that I8 certifies none of the retained payload.

## 2. Sync The Documentation-Only Capability

- [x] 2.1 Sync the private inventory-classifier payload authorization
  capability without changing product, test, protocol, provider or runtime
  requirements.
- [x] 2.2 Update only I8 card/change/archive metadata required for the exact
  source and preserve Apache-2.0 unchanged.

## 3. Verify And Archive

- [x] 3.1 Audit exact JSON keys/values, exact dependency/block identities and
  the required independent-review-before-I4-reference condition.
- [x] 3.2 Prove the payload is docs/OpenSpec-only and contains no I4,
  product/test, runtime-evidence or external-operation byte; protocol evidence
  generation is not applicable because no protocol knowledge changes.
- [x] 3.3 Run strict change/capability/all OpenSpec validation, docs-only scope,
  license, public-surface/privacy and tracked-plus-untracked whitespace checks.
- [x] 3.4 Record Windows-native/test-first/live verification as prohibited and
  not applicable, archive the completed change and retain the card in
  `3.inprogress` for fresh independent review.

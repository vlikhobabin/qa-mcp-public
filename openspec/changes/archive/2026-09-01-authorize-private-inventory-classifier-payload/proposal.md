## Why

Published I7 intentionally leaves exact I4 blocked until a separate unchanged
tracked authorization source binds the investigation and successor through the
machine-readable ChangeRail contract. I8 must publish that exact source without
touching or certifying the retained I4 payload.

## What Changes

- Publish the exact six-field investigation authorization prepared by I7,
  binding only the unchanged published I7 card and exact I4 id/path with
  `production_loc_ceiling: 301` and no new authority or wire protocol.
- Require I8 to depend on I7 and remain independently reviewed and published
  unchanged in `4.done` before I4 may reference it.
- Preserve I7's future I4 admission conditions without claiming that its
  retained path set, LOC, blobs or hostile/offline verification already pass.
- Touch only the OpenSpec workflow, documentation and board metadata. Do not
  change protocol tools, Python manager/MCP code, provider setup, tests or
  runtime lab configuration.

## Capabilities

### New Capabilities
- `qa-mcp-private-inventory-classifier-payload-authorization`: Defines the
  exact non-reusable I7-to-I4 authorization source and its fail-closed,
  documentation-only publication boundary.

### Modified Capabilities
- None.

## Impact

The change affects only this card, one OpenSpec authorization change and its
synced capability. It adds no product/test implementation, protocol or runtime
behavior, and preserves Apache-2.0 unchanged. It requires no live 1C runtime,
Vanessa MCP, EDT/meta snapshot, offline capture evidence, Windows or PowerShell
execution, endpoint/target access, SSH or external operation.

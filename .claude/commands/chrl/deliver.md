Run the project-local `./bin/chrl-run <todo-card>` for $ARGUMENTS only when
the user authorized delivery including commit and push. The common local runtime
owns native implementation/review; Claude does not write a review verdict.
At most two independent reviews cover semantic and failed-floor repair together.
Read AGENTS.md and docs/development/local-changerail-delivery.md.
